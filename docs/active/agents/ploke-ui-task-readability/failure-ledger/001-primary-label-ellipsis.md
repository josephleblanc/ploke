# 001: Label Ellipsis

Primary Artifact labels were shortened into indistinguishable handles like
`A:text-fi...`.

Relevant code:

- `crates/ploke-egui/src/ui/view/projection.rs`
- `artifact_label(...)`
- `compact_id(...)`
- `truncate_label(...)`
- `MAX_PRIMARY_LABEL_CHARS`
