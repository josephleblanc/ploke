# S3D Restart Rubric Sample Report

## implemented

- Sampled three recent recovery artifacts: [CURRENT_FOCUS.md](/home/brasides/code/ploke/docs/active/CURRENT_FOCUS.md:8), the [eval-infra sprint control plane](/home/brasides/code/ploke/docs/active/agents/2026-04-12_eval-infra-sprint/2026-04-12_eval-infra-sprint-control-plane.md:95), and [recent-activity.md](/home/brasides/code/ploke/docs/active/workflow/handoffs/recent-activity.md:18).
- Applied a simple restart rubric across `context recovery`, `decision locality`, and `next-step discoverability`.
- Kept the recommendation narrow: the sample does not justify a new workflow process, only a possible micro-refinement if future restart samples still miss the pointer chain.

## claims

- [AC1] The sampled artifacts are recent, real, and sufficient to exercise restart from the current eval-infra sprint state without depending on chat retention.
- [AC2] `CURRENT_FOCUS.md` is the strongest context-recovery entry point because it names the active sprint, the next decision, and the exact follow-on packets.
- [AC2] The control plane is the strongest decision-locality surface because it colocates lane status, packet acceptance boundaries, and the current-state table.
- [AC2] `recent-activity.md` is useful chronology, but it is weaker than the other two for immediate restart because the operator still has to synthesize across entries.
- [AC3] The smallest supported process change is no new process change yet; the current three-doc recovery path is working well enough for this sample. If a later sample still shows scan friction, the narrowest follow-up would be a tiny `restart snapshot` block at the top of `recent-activity.md` pointing to the active focus and control plane.

## evidence

- Sampled artifacts:
  - [CURRENT_FOCUS.md](/home/brasides/code/ploke/docs/active/CURRENT_FOCUS.md:8)
  - [Eval Infra Sprint Control Plane](/home/brasides/code/ploke/docs/active/agents/2026-04-12_eval-infra-sprint/2026-04-12_eval-infra-sprint-control-plane.md:95)
  - [Recent Activity](/home/brasides/code/ploke/docs/active/workflow/handoffs/recent-activity.md:18)
- Rubric dimensions used:
  - `context recovery`: does the artifact let a replacement orchestrator reconstruct the sprint state quickly?
  - `decision locality`: are the active decisions, boundaries, and dependencies visible in one place?
  - `next-step discoverability`: can the next action be found without chasing multiple docs or chat history?
- Score summary:
  - `CURRENT_FOCUS.md`: context recovery `5/5`, decision locality `3/5`, next-step discoverability `5/5`
  - Control plane: context recovery `4/5`, decision locality `5/5`, next-step discoverability `4/5`
  - `recent-activity.md`: context recovery `4/5`, decision locality `4/5`, next-step discoverability `3/5`
- High-signal lines in the sample:
  - `CURRENT_FOCUS.md` explicitly names the active sprint, accepted P0 closure, and the current next packets (`S1D`, `S2D`, `S3D`) at lines 10-28.
  - The control plane’s current-state table and immediate orchestrator guidance localize accepted packets, ready follow-ups, and the resume path at lines 95-155.
  - `recent-activity.md` preserves the chronological record of protocol adoption, packet acceptance, and follow-up seeding, but it is less direct as a restart surface than the first two docs.

## unsupported_claims

- I did not sample any chat transcripts, so I did not test whether chat-based reconstruction is better or worse than the documented path.
- I did not measure time-to-restart or operator error rate.
- I did not compare against an older sprint, so this is a point sample rather than a trend analysis.

## not_checked

- Whether a later restart sample would prefer `S1D`, `S2D`, or `S3D` as the next operational packet.
- Whether any other workflow document outside the three sampled artifacts would materially improve restart locality.
- Whether the proposed `restart snapshot` block would be useful enough to justify adding it now.

## risks

- `recent-activity.md` is still useful, but if it becomes the only recovery artifact an operator reads, restart quality will likely drop.
- The current path depends on two primary docs rather than one single canonical snapshot, so future drift in either file would reduce recovery quality.
- The recommendation is based on a small sample, so it should be revisited if later sidecar work changes the recovery shape.

## next_step

- Keep the current recovery chain unchanged for now, and only add a tiny `restart snapshot` block to `recent-activity.md` if a later sample shows operators still need an extra pointer to the active focus and control plane.
