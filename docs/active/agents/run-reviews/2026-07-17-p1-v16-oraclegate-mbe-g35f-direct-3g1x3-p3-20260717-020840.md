# P1 v16 All-Unresolved Oracle-Gate Run Review

Campaign:
`p1-v16-oraclegate-mbe-g35f-direct-3g1x3-p3-20260717-020840`

Status: preserved negative live proof; stopped at R12 under the pre-receipt
binary; no successor handoff.

## Review Verdict

The strict oracle gate worked. The only admitted child was operationally
`keep`, but its MBE verdict was `unresolved`, so `all-resolved` excluded it from
successor selection. The run did not corrupt History, install a checkout, or
spawn a successor.

The run also exposed a real observability/reconstruction gap in that binary:
selection completed without an admissible candidate, but no typed passive
selection receipt or receipt-bound stopped successor record was persisted.
Because the admitted source epoch cannot be changed in place, this campaign is
preserved at R12 and is not eligible for retrofit or resume.

## Admitted Policy

The committed run profile used direct Google Gemini 3.5 Flash with:

```toml
[selection.oracle]
mode = "record-only"
require_evidence = true
gate = "all-resolved"

[execution.mbe]
enabled = true

[search]
max_generations = 3
max_total_nodes = 10
```

The broad harness used three parallel targets, one fresh slot per child, a
1,800-second timeout, and control parallelism of three.

## Child Planning and Execution

Parent `node-e4ecdce2d6ee1098` produced one admitted child and two rejected
surface attempts.

- Admitted child: `node-0d80c1aeea697e6b`
- Branch: `branch-652e6dab480c355a`
- Derived commit: `48315c6abbe081d8da3bbe7df5c79e9afe37425a`
- Target: `crates/ploke-ty-mcp/src/manager.rs`
- Child runtime status: `succeeded`
- Branch disposition: `keep`

The r2 and r3 broad-harness proposals were rejected after their requested
`cargo check -p ploke-eval` validations failed. Their evidence remains in the
child-plan and headless-TUI result artifacts; neither rejected attempt received
a child runtime.

## Oracle Evidence

For `BurntSushi__ripgrep-2209`, the branch evaluation recorded:

- MBE evidence usable for selection;
- verdict `unresolved`;
- `regression::r2095` fixed; and
- `regression::r2208` still failing.

This is the intended negative case: operational improvement did not override
the stricter benchmark-resolution admission rule.

## Persisted Boundary

The production transition journal contains no successor entries and no History
entries for this campaign. No History block exists. The last journal event is a
resource event after child observation, consistent with the run reaching the
selection/continuation boundary without committing a successor handoff.

The pre-fix binary did not persist enough typed evidence to reconstruct the
R12-to-R13a stop. Rewriting the journal, profile, source epoch, or digest would
invalidate the operational proof, so the run remains untouched.

## Historical Regression

The immutable fixture
`crates/ploke-eval/src/tests/fixtures/prototype1-v16-all-unresolved-20260717/`
pins the production carriers by SHA-256 and replays them through current
production selection code. The regression verifies:

- pure selection leaves the owner DB unchanged;
- the result is a passive schema-v5 no-selection receipt;
- one decision, one candidate, one score, and one oracle row are queryable;
- four candidate projection/traversal failures are queryable;
- the candidate is unselectable because `oracle_gate_not_satisfied`;
- R12 records `Completed { hash }` for the exact receipt; and
- History, checkout, runtime identity, and successor handoff remain absent.

A separate rejected-only regression records `NotRun`, preserving the semantic
difference between “selection completed with no candidate” and “selection was
not attempted.”

## Next Live Proof

Use a fresh campaign from the repaired source. Completion requires a resolved
candidate to pass the same strict gate, seal through the existing History and
digest verification, hand control to the verified successor runtime, and let
that successor advance another generation without rewriting earlier evidence.
