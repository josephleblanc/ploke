# Broad Harness Attempt Review: r3

Campaign: `p1-gemini35-flash-direct-2g1x3-20260525-073410`
Attempt: `broad-harness-request:node-b2a63be3e50c7ee8:r3`
Reviewed: 2026-05-25

## Verdict

r3 was mechanically active but not a completed broad-harness artifact. The headless TUI trace shows useful repository exploration and four applied edit proposals, but the terminal record is `timed_out` after 900 seconds, the requested submitted result file is absent, and the checkout is still dirty at the original target artifact commit.

Do not treat this attempt as an admitted/submitted child. It is useful as trace evidence for model behavior and edit-surface failure modes, but benchmark outcome is absent for this slot.

## Evidence Roots

- Campaign root: `/home/brasides/.ploke-eval/campaigns/p1-gemini35-flash-direct-2g1x3-20260525-073410`
- Request: `prototype1/messages/edit-harness-request/node-b2a63be3e50c7ee8-r3.json`
- Headless result: `prototype1/messages/edit-harness-result/node-b2a63be3e50c7ee8-r3.headless-tui.json`
- Requested submitted result path: `prototype1/messages/edit-harness-result/node-b2a63be3e50c7ee8-r3.json`
- Candidate workspace: `prototype1/workspaces/edit-harness/node-b2a63be3e50c7ee8-r3`
- Runtime-playback inventory checked before missing-record claims:
  `docs/workflow/evalnomicon/drafts/observability/runtime-playback/inventory/README.md`,
  `record-surface-map.md`, and `latest-run-emission-worksheet.md`.

## Closure State

Campaign closure is complete for the main eval run and partial for protocol:
registry `complete`, eval `complete`, protocol `partial`. The closure state lists one protocol artifact, the intent segmentation file, and marks tool-call review plus segment review missing.

That campaign-level closure does not make r3 complete. r3 is a broad-harness attempt artifact with its own request/result slot; its terminal state is timeout and its submitted result file is absent.

## Eval And Patch Output

The request bound the attempt to target artifact `artifact:git-commit:bf8269e1511dfcb27c7b91dfca56ea290464413a`, runtime `7f522409-04d2-4095-95c2-1f8deed936f9`, policy `workspace except ploke-eval`, and workspace `node-b2a63be3e50c7ee8-r3`.

Checkout evidence:

- Branch: `prototype1-broad-broad-harness-request-node-b2a63be3e50c7ee8-r3`
- HEAD: `bf8269e1511dfcb27c7b91dfca56ea290464413a`
- Dirty files:
  - `crates/ploke-tree/src/graph/artifact_tree.rs`
  - `crates/ploke-tree/src/graph/types/child_plan.rs`
  - `crates/ploke-tree/src/graph/types/parent_create.rs`
- Diff stat: 3 files, 105 insertions, 54 deletions.

The diff attempted to optimize artifact-tree construction by pre-indexing child plans and candidates, replacing repeated graph scans with maps, and exposing `parent_create_from_child` as `pub(crate)`. One patch also changed `ChildPlanIndex::plan_for_parent_node_id`, but the resulting hunk has suspicious indentation and constructs `SchedulerNodeId(node_id.to_owned())` directly. This was not validated by a final successful test run after all edits.

## Oracle And MBE State

No r3 submitted result exists at the request's `submitted_result_path`. The record label for this attempt is `record absent`: the expected broad-harness submitted result file was not written.

The campaign-level benchmark run root does have `record.json.gz`, `agent-turn-*`, `llm-full-responses.jsonl`, `multi-swe-bench-submission.jsonl`, and `benchmark-patch-projection.json`, but those are not r3's broad-harness submission. They are `record present, manual join needed` campaign context for this review.

## LLM And Tool Behavior

The r3 headless trace has 109 compact events:

- 50 tool requests
- 52 tool completions
- 3 tool failures
- 4 proposal events

Tool requests by family:

- `read_file`: 21
- `request_code_context`: 10
- `list_dir`: 9
- `cargo`: 4
- `non_semantic_patch`: 3
- `apply_code_edit`: 2
- `code_item_lookup`: 1

The model initially explored `crates/ploke-tree`, then moved into artifact-tree and child-plan graph code. It also inspected the parent node evidence under the campaign nodes directory successfully before one later absolute campaign-root `list_dir` failed as outside configured roots.

## Trace Reconstruction

The active execution path for this artifact is the published broad headless-TUI path:

```text
prototype1-harness attempt / published request
-> tui_adapter::run_headless_with_model
-> compact headless result JSON
-> broad-harness submitted result only if terminal applied/submitted
```

The turning points were:

1. The model ran focused `cargo check` in `crates/ploke-tree`; it passed.
2. It ran focused `cargo test`; this failed at compile/link with one error, not from the later candidate diff.
3. It applied a patch to `child_plan.rs`.
4. An `apply_code_edit` against `parent_create_from_child` failed with no matching node, then a `non_semantic_patch` succeeded.
5. It applied two patches to `artifact_tree.rs`.
6. It ran focused `cargo check` twice successfully after intermediate edits.
7. The run timed out before submission, final validation, or result materialization.

The timeout matters because the last visible trace had a large retained context and pending edit work. The harness had applied proposals, but there is no submitted result and no committed candidate.

## Protocol Review And Blind Spots

Protocol artifacts are partial at the campaign level: intent segmentation exists; tool-call review and segment review are missing. For r3 specifically, there is no per-attempt protocol review artifact.

The headless result is therefore the authority for this attempt's tool lifecycle. It is an `operator/convenience record` plus compact diagnostic trace, not a full eval run record equivalent to `agent-turn-trace.json` plus `llm-full-responses.jsonl`.

## What Worked

- The model found the relevant `ploke-tree` artifact-tree area and made a plausible optimization attempt.
- Failed semantic edit targeting was recoverable through `non_semantic_patch`.
- Focused `cargo check` did catch compile viability at intermediate points.

## What Did Not Work

- The attempt missed its own terminal contract: no submitted result file and no clean committed child.
- The model spent enough time in broad exploration and patch repair to hit the 900 second timeout.
- The final dirty diff includes at least one suspicious hunk in `child_plan.rs`.
- The validation target drifted from the request contract. The request asked for `cargo check -p ploke-eval` and `cargo test -p ploke-eval edit_surface`; the trace mostly ran focused `ploke-tree` checks.

## Action Items

- Treat r3 as a failed timed-out attempt, not as a child candidate.
- Keep the r3 headless result as replay/debug evidence for edit-surface and timeout behavior.
- If harvesting useful ideas, manually review the `artifact_tree.rs` optimization separately; do not import the dirty checkout wholesale.
- Consider a harness-side terminal record that explicitly distinguishes `applied but unsubmitted` from `submitted`, so downstream selection cannot over-credit dirty workspaces.
