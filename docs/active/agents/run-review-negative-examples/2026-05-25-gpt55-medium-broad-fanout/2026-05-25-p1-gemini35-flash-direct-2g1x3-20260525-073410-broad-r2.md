# p1-gemini35-flash-direct-2g1x3 Broad Harness r2 Attempt Review

Date: 2026-05-25
Campaign: `p1-gemini35-flash-direct-2g1x3-20260525-073410`
Attempt: `broad-harness-request:node-b2a63be3e50c7ee8:r2`
Instance: `BurntSushi__ripgrep-2209`

## Verdict

The r2 attempt is mechanically cleaner than the base trace but reaches the same bad outcome. It produced a headless TUI trace and applied two proposals, yet it did not write the requested submitted-result JSON, terminated as `tool_failed`, and left only diagnostic search tests in `crates/ploke-tree/src/tests.rs`.

The attempt found some relevant artifact-tree and descendant-workspace code, but did not turn that context into a real performance fix. It should not be treated as a valid benchmark candidate.

## Evidence Roots

- Campaign root: `/home/brasides/.ploke-eval/campaigns/p1-gemini35-flash-direct-2g1x3-20260525-073410`
- Request: `prototype1/messages/edit-harness-request/node-b2a63be3e50c7ee8-r2.json`
- Headless result / TUI trace: `prototype1/messages/edit-harness-result/node-b2a63be3e50c7ee8-r2.headless-tui.json`
- Submitted result expected by request: `prototype1/messages/edit-harness-result/node-b2a63be3e50c7ee8-r2.json`
- Candidate checkout: `prototype1/workspaces/edit-harness/node-b2a63be3e50c7ee8-r2`
- Campaign run root checked for inventory context: `/home/brasides/.ploke-eval/instances/prototype1/p1-gemini35-flash-direct-2g1x3-20260525-073410/BurntSushi__ripgrep-2209/runs/run-1779695507168-structured-current-policy-58118bd8`

## Execution Path

This artifact came from the published broad headless-TUI request path:

```text
prototype1-harness attempt -> tui_adapter::run_headless_with_model -> headless ploke-tui event stream -> edit-harness-result/*.headless-tui.json
```

The campaign's full eval run root is present and separately auditable, but the r2 headless artifact is the authority for this attempt's TUI behavior.

## Request Slot, Workspace, Branch, Commit

- Request id: `broad-harness-request:node-b2a63be3e50c7ee8:r2`
- Admission runtime id: `7f522409-04d2-4095-95c2-1f8deed936f9`
- Target artifact: `artifact:git-commit:bf8269e1511dfcb27c7b91dfca56ea290464413a`
- Policy: `workspace except ploke-eval`
- Workspace loaded by TUI: `.../prototype1/workspaces/edit-harness/node-b2a63be3e50c7ee8-r2`
- Focused root: `.../node-b2a63be3e50c7ee8-r2/crates/ploke-tree`
- Git branch checked in the workspace: `prototype1-broad-broad-harness-request-node-b2a63be3e50c7ee8-r2`
- Git HEAD: `bf8269e1511dfcb27c7b91dfca56ea290464413a`
- Checkout status: dirty, with only `crates/ploke-tree/src/tests.rs` modified.

## Closure And Campaign State

The campaign manifest records model `google/gemini-3.5-flash`, route source `direct_google`, benchmark family `multi_swe_bench_rust`, and required protocol procedures `tool-call-intent-segments`, `tool-call-review`, and `tool-call-segment-review`.

`closure-state.json` reports eval complete and protocol partial for `BurntSushi__ripgrep-2209`. The protocol projection says segmentation completed, call review is missing, segment review is missing, and all 12 segments are missing review.

## Record Inventory Labels

- `operator/convenience record`: `node-b2a63be3e50c7ee8-r2.headless-tui.json` is the compact per-attempt TUI trace.
- `record absent`: `node-b2a63be3e50c7ee8-r2.json`, the submitted-result path named in the request, does not exist.
- `record present, manual join needed`: the campaign run root has full eval artifacts, including `record.json.gz`, agent-turn records, raw provider responses, validation audit, patch projection, and MBE submission. These were joined by campaign and instance path, not by a first-class broad-attempt playback relation.
- `record present, playback gap`: the protocol root has a `tool_call_intent_segmentation` artifact, but closure reports missing call and segment review.

## LLM And Tool Behavior

The r2 headless trace contains 68 events: 32 `tool_request`, 33 `tool_completed`, 1 `tool_failed`, and 2 `proposal` events.

Compared with the base attempt, r2 reached the temporary-test pattern faster. It listed the workspace, ran `cargo test -p ploke-tree`, tried to inspect prior nodes with a relative path, read `AGENTS.md` and `TECH_DEBT.md`, used `request_code_context` for `descendant`, `ancestor`, `performance`, `benchmark`, and `ArtifactTree`, and read `crates/ploke-tree/src/graph/artifact_tree.rs`.

The only explicit tool failure was:

- `list_dir` on `../../../../nodes`, rejected because it resolved outside configured roots.

The model recovered from that failure by continuing local inspection, but again used failing diagnostic tests as its main search mechanism.

## Edit And Validation Trace

Two edit proposals were applied:

- `35366dd3-9665-5026-9709-c59befc9e260`
- `29834ec2-8d62-5322-bc87-046f34f52dc0`

Both modified `crates/ploke-tree/src/tests.rs`. The checkout diff appends two duplicate `#[test] fn find_all_occurrences_of_descendant()` functions. Each recursively walks the broad-harness workspace and panics to print files or line matches containing `descendant`. The second version excludes paths containing `ploke-eval`, but both functions remain in the file and neither is a product fix.

Validation evidence:

- `cargo test -p ploke-tree`: passed before the diagnostic tests were inserted.
- `cargo test -p ploke-eval`: failed with exit code 101 and 486 warnings.
- `cargo test -p ploke-tree -- find_all_occurrences_of_descendant`: failed by design because the temporary diagnostic test panicked.

The final checkout is dirty and benchmark-hostile. It adds failing tests and no optimization.

## Positive Examples And Adjudication Candidates

The useful part of r2 is a shorter localization path. The model found `ArtifactTree`, read `artifact_tree.rs`, and used the diagnostic test output to enumerate descendant-related files. This is evidence of context discovery.

The adjudication field should be explicit: `context discovery did not produce cleanup or patch`. The run should not receive positive benchmark-credit merely for finding relevant files.

## Protocol Review And Blind Spots

The campaign run trace audit reported 93 provider responses, 92 provider-emitted tool calls, 92 recorded tool calls, and 0 missing recorded provider call ids for the full eval run root. It also classified recorded calls as context results, completed calls, reads with content, transport failures, empty completed reads, and duplicate requests.

That audit confirms the full campaign run is not missing its primary model/tool records. It does not repair the r2 attempt's missing submitted-result JSON or the fact that the per-attempt checkout only contains failing diagnostic tests.

## What Is Working

- The r2 request has a stable request id, request hash, admission binding, target artifact, prompt path, and expected submitted-result path.
- The headless trace records the model's tool sequence, prompt diagnostics, applied proposal ids, validation commands, and terminal failure.
- The candidate checkout is inspectable and its dirty diff exactly identifies the applied content.

## What Is Not Working Yet

- The submitted-result JSON for r2 is absent.
- The terminal state is `tool_failed` because the headless event stream hit `scan barrier failed: channel closed`.
- The model repeated the base attempt's pattern of inserting diagnostic tests and leaving them in place.
- The validation sequence includes pre-edit success but ends with deliberate post-edit test failure.

## Action Items

1. Treat r2 as a failed attempt, independent of the base attempt.
2. Add or update a blocker-repair item for headless attempts that apply proposals but fail to write submitted-result artifacts.
3. Add an adjudication penalty for leaving temporary diagnostic tests in the candidate checkout.
4. Consider adding a harness-level cleanup check: if the final diff only adds failing tests or panic-based search probes, reject before candidate selection.
