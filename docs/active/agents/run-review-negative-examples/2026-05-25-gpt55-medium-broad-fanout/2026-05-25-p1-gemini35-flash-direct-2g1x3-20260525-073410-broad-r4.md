# Broad Harness Attempt Review: r4

Campaign: `p1-gemini35-flash-direct-2g1x3-20260525-073410`
Attempt: `broad-harness-request:node-b2a63be3e50c7ee8:r4`
Reviewed: 2026-05-25

## Verdict

r4 is mechanically complete as a broad-harness attempt: the headless TUI terminal state is `applied`, the submitted result file exists, the workspace is clean, and the candidate commit is present. The semantic quality is weaker than the mechanical status. The model eventually made a small `ploke-db` performance patch, but it also followed several low-signal paths, hit five tool failures, ran validation commands that did not match the request contract, and recorded a failing `cargo test -p ploke-db` before submission.

This attempt should be considered a submitted candidate with caveats, not a proven benchmark improvement.

## Evidence Roots

- Campaign root: `/home/brasides/.ploke-eval/campaigns/p1-gemini35-flash-direct-2g1x3-20260525-073410`
- Request: `prototype1/messages/edit-harness-request/node-b2a63be3e50c7ee8-r4.json`
- Headless result: `prototype1/messages/edit-harness-result/node-b2a63be3e50c7ee8-r4.headless-tui.json`
- Submitted result: `prototype1/messages/edit-harness-result/node-b2a63be3e50c7ee8-r4.json`
- Candidate workspace: `prototype1/workspaces/edit-harness/node-b2a63be3e50c7ee8-r4`
- Runtime-playback inventory checked before missing-record claims:
  `docs/workflow/evalnomicon/drafts/observability/runtime-playback/inventory/README.md`,
  `record-surface-map.md`, and `latest-run-emission-worksheet.md`.

## Closure State

Campaign closure is complete for registry and eval, partial for protocol:

- registry: expected 1, mapped 1, status `complete`
- eval: expected 1, complete 1, status `complete`
- protocol: expected 1, full 0, partial 1, status `partial`
- protocol procedures: intent segmentation complete; tool-call review and segment review missing

For this r4 attempt, the broad-harness slot itself completed and submitted a result. Campaign protocol partiality remains a review limitation, not proof that r4 failed mechanically.

## Eval And Patch Output

The request bound r4 to target artifact `artifact:git-commit:bf8269e1511dfcb27c7b91dfca56ea290464413a`, runtime `7f522409-04d2-4095-95c2-1f8deed936f9`, policy `workspace except ploke-eval`, and workspace `node-b2a63be3e50c7ee8-r4`.

Checkout evidence:

- Branch: `prototype1-broad-broad-harness-request-node-b2a63be3e50c7ee8-r4`
- Candidate commit: `ce3e0f713ef29af9089964bffac9fbb69988242c`
- Commit subject: `prototype1 broad harness result broad-harness-request:node-b2a63be3e50c7ee8:r4`
- Dirty diff after commit: none
- Commit stat: 2 files, 8 insertions, 7 deletions

Changed files:

- `crates/ploke-db/src/database.rs`
- `crates/ploke-db/src/helpers.rs`

The patch changes recursive Datalog rules. In `retract_embedded_files`, it replaces a global `ancestor` relation with a file-bounded `descendant_of_file` recursion rooted at `file_mod`. In `lookup_ancestor_rules_now`, it inlines `parent_of` and rewrites `ancestor` as left-recursive through the already-bound descendant side.

The submitted result records the same changed files and claims a bounded self-edit for descendant evaluation. It does not claim admission, grant, or child-plan authority.

## Oracle And MBE State

The r4 submitted result exists and is `record present, manual join needed`: it is a broad-harness result file, and the reviewer had to join it manually to request slot, workspace, branch, commit, terminal state, and checkout diff.

Campaign-level benchmark evidence exists separately under the instance run root:

- `record.json.gz`
- `agent-turn-trace.json`
- `agent-turn-summary.json`
- `llm-full-responses.jsonl`
- `multi-swe-bench-submission.jsonl`
- `benchmark-patch-projection.json`

The bundled trace audit over that run root reported 93 provider responses, 92 provider-emitted tool calls, 92 recorded tool calls, and zero missing provider call ids. That is useful campaign context, but it is not the r4 broad attempt trace.

The benchmark patch projection for the Multi-SWE-bench ripgrep instance passed export checks, with base SHA `4dc6c73c5a9203c5a8a89ce2161feca542329812`. That benchmark patch edits ripgrep files, not the r4 broad-harness `ploke-db` candidate.

## LLM And Tool Behavior

The r4 headless trace has 228 compact events:

- 110 tool requests
- 110 tool completions
- 5 tool failures
- 2 proposal events
- 1 turn event

Tool requests by family:

- `read_file`: 61
- `list_dir`: 22
- `request_code_context`: 13
- `cargo`: 8
- `apply_code_edit`: 3
- `code_item_lookup`: 2
- `code_item_edges`: 1

The trace starts in `ploke-tree-egui`, wanders through parser visitor code and broad performance searches, then eventually localizes to `ploke-db` Datalog query performance. That later turn is the useful part.

## Trace Reconstruction

The active execution path for this artifact is the published broad headless-TUI path:

```text
prototype1-harness attempt / published request
-> tui_adapter::run_headless_with_model
-> compact headless result JSON
-> submitted broad-harness result JSON
-> candidate commit in attempt workspace
```

Turning points:

1. Initial `cargo check`, `cargo test`, and `cargo test --tests` against the focused `ploke-tree-egui` manifest passed, but this did not validate the final `ploke-db` change.
2. Several early reads targeted nonexistent or wrong parser paths, and one campaign `list_dir` failed outside configured roots.
3. A broad search for `performance` found an unrelated LLM metadata type, which did not directly guide the final patch.
4. The model eventually inspected `crates/ploke-db/src/database.rs` and `helpers.rs`, identified recursive Datalog query costs, and applied two edit proposals.
5. Post-edit `cargo check` passed twice against the focused `ploke-tree-egui` manifest while compiling `ploke-db` as a dependency.
6. `cargo test -p ploke-tree` passed twice.
7. `cargo test -p ploke-db` failed with exit code 101 and five warnings. The compact result does not include the failing test name.
8. The headless terminal still reached `applied`, and the workspace committed the candidate as `ce3e0f713ef29af9089964bffac9fbb69988242c`.

## Protocol Review And Blind Spots

Protocol review is partial for the campaign and absent at r4 attempt granularity. There is one campaign protocol intent segmentation artifact, but no tool-call review or segment review artifacts. The r4 review therefore relies on:

- request JSON as the durable contract,
- headless result JSON as compact terminal/TUI trace,
- submitted result JSON as candidate return evidence,
- git commit and checkout status as patch authority.

The compact headless result is not equivalent to full eval `agent-turn-trace.json` and `llm-full-responses.jsonl`; the inventory labels it best as `operator/convenience record` plus manual-join evidence for broad attempts.

## What Worked

- The attempt produced a clean committed candidate and a submitted result file.
- The final patch is small and in the allowed surface outside `crates/ploke-eval`.
- The model used validation feedback enough to rerun checks after edits.
- The idea is plausible: bounding recursive Datalog traversal to a known file/module can reduce whole-graph transitive closure work.

## What Did Not Work

- Validation did not follow the request contract. The request asked for `cargo check -p ploke-eval` and `cargo test -p ploke-eval edit_surface`; neither appears in the r4 validation list.
- `cargo test -p ploke-db` failed after the candidate edits, and the attempt still submitted.
- Early exploration was noisy and included empty/truncated reads, nonexistent paths, and unrelated performance context.
- The submitted result's rationale is generic and does not cite the failing `ploke-db` test or explain why the failure is acceptable.

## Action Items

- Treat r4 as a mechanically submitted candidate with red validation, not as a proven improvement.
- Before selection/admission, rerun the request-specified checks in the candidate workspace: `cargo check -p ploke-eval` and `cargo test -p ploke-eval edit_surface`.
- Also rerun or inspect `cargo test -p ploke-db` because the headless trace recorded that target as failing.
- Improve broad-harness submitted-result admission so a failed post-edit validation is surfaced in the submitted JSON, not only in the compact headless result.
