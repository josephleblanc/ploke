# Prototype 1 Broad-Harness Attempt Review: `p1-gemini35-flash-direct-2g1x3-20260525-073410` r5

## Verdict

The r5 broad-harness attempt produced useful trace evidence but not an
admissible child result. The headless-TUI artifact is present and mechanically
complete enough to review, but the attempt terminal state is `timed_out` after
900 seconds, the request-declared submitted result file is absent, and the
candidate checkout was left dirty rather than committed.

The workspace branch is
`prototype1-broad-broad-harness-request-node-b2a63be3e50c7ee8-r5`; `HEAD`
remains the parent commit `bf8269e1511dfcb27c7b91dfca56ea290464413a`. The
dirty diff changes only `crates/ploke-ty-mcp/src/manager.rs` with 90 insertions
and 44 deletions. The last recorded validation was a focused `cargo check`
against `crates/ploke-ty-mcp/Cargo.toml`, and it failed with 16 errors.

This is benchmark-useless as a candidate artifact. It is still useful as
failure evidence: the model found a plausible MCP startup/concurrency target,
but the edit lifecycle thrashed across repeated same-file patches and left
Rust-2024 `gen` keyword errors in the final checkout.

## Evidence Roots

- Campaign:
  `/home/brasides/.ploke-eval/campaigns/p1-gemini35-flash-direct-2g1x3-20260525-073410`
- Parent node: `node-b2a63be3e50c7ee8`
- Request slot:
  `prototype1/messages/edit-harness-request/node-b2a63be3e50c7ee8-r5.{json,md}`
- Headless result:
  `prototype1/messages/edit-harness-result/node-b2a63be3e50c7ee8-r5.headless-tui.json`
- Request-declared submitted result:
  `prototype1/messages/edit-harness-result/node-b2a63be3e50c7ee8-r5.json`
- Workspace:
  `prototype1/workspaces/edit-harness/node-b2a63be3e50c7ee8-r5`
- Branch:
  `prototype1-broad-broad-harness-request-node-b2a63be3e50c7ee8-r5`
- Parent commit:
  `bf8269e1511dfcb27c7b91dfca56ea290464413a`

I did not advance the loop, rerun cargo in the candidate workspace, commit the
dirty branch, or update the run-review README. This review uses persisted
artifacts plus read-only git inspection.

## Execution Path

This is the broad-harness child attempt path:

```text
prototype1 child-plan broad-harness request
  -> tui_adapter::run_headless_with_model
  -> headless-TUI result artifact
  -> dirty attempt workspace
```

The request binding targeted artifact
`artifact:git-commit:bf8269e1511dfcb27c7b91dfca56ea290464413a` under policy
`workspace except ploke-eval`. The request asked for edits outside protected
core and listed the intended validation contract as:

```text
cargo check -p ploke-eval
cargo test -p ploke-eval edit_surface
```

The recorded cargo calls did not run that contract. They resolved to the
focused `crates/ploke-ty-mcp/Cargo.toml` manifest.

## Record Inventory Classification

- `record present, manual join needed`: the request JSON, prompt markdown,
  headless result JSON, terminal object, validation list, compact TUI events,
  prompt diagnostics, and attempt workspace all exist, but the review had to
  join them manually by request slot and workspace path.
- `record absent`: the request-declared submitted result
  `node-b2a63be3e50c7ee8-r5.json` was not written.
- `record present, playback gap`: the headless-TUI event stream exists, but it
  is a compact diagnostic trace rather than a full `agent-turn-trace.json` plus
  `llm-full-responses.jsonl` playback surface for this child attempt.
- `operator/convenience record`: the checkout diff and branch/HEAD inspection
  are git-derived review evidence, not typed campaign records.

The campaign closure record itself exists and reports eval `complete` but
protocol `partial`: intent segmentation completed, while tool-call review and
segment review are missing for the completed eval run. That protocol state is
campaign context, not evidence that r5 produced a valid child artifact.

## Attempt Output

Terminal state:

```text
timed_out, secs = 900
```

Result artifact counts:

- 7 attempt records.
- 133 compact events.
- 61 `tool_request` events.
- 64 `tool_completed` events.
- 2 `tool_failed` events.
- 6 proposal events.
- 3 cargo validations.

Tool request mix:

- `read_file`: 28
- `list_dir`: 14
- `request_code_context`: 9
- `non_semantic_patch`: 4
- `cargo`: 3
- `code_item_lookup`: 2
- `apply_code_edit`: 1

The prompt diagnostics show context mode `Off`, workspace loaded, 27 members,
focused root `crates/ploke-ty-mcp`, BM25 ready with 6512 docs, and no included
RAG parts.

## Checkout Diff

The final dirty checkout changes one file:

```text
crates/ploke-ty-mcp/src/manager.rs | 134 +++++++++++++++++++++++++------------
1 file changed, 90 insertions(+), 44 deletions(-)
```

The change attempts to:

- store `(RunningService<RoleClient, ()>, usize)` in the MCP registry;
- add per-server startup locks and generation counters;
- serialize startup and respawn;
- make tool/list calls aware of the service generation;
- defer the command-on-PATH preflight until after child process creation fails;
- stub `health_check` to always return `Ok(())`.

The direction is performance/reliability motivated, but the final diff is not
ready. It disables the health check entirely and leaves compile errors.

## Validation

Recorded validations:

- `function-call-0dbc3a46-34d1-4dd5-8e53-2012c6cdeb9b`: focused
  `cargo check`, manifest `crates/ploke-ty-mcp/Cargo.toml`, success.
- `function-call-08eb2193-272b-4719-bfea-48f62cc6d925`: focused
  `cargo test`, manifest `crates/ploke-ty-mcp/Cargo.toml`, success.
- `function-call-1716ddce-3674-473e-9d2c-523daedeeb86`: focused
  `cargo check`, manifest `crates/ploke-ty-mcp/Cargo.toml`, failed with 16
  errors and 2 warnings.

The final validation failure includes Rust-2024 keyword errors such as:

```text
crates/ploke-ty-mcp/src/manager.rs:74:13
expected identifier, found reserved keyword `gen`

crates/ploke-ty-mcp/src/manager.rs:75:45
expected expression, found reserved keyword `gen`

crates/ploke-ty-mcp/src/manager.rs:183:26
expected identifier, found reserved keyword `gen`
```

Earlier passing checks do not rescue the final state. The checkout at timeout
was dirty and uncompilable.

## Trace Reconstruction

The model began with useful localization: it searched for `McpManager`, found
`crates/ploke-ty-mcp/src/manager.rs`, inspected the manager/config/type files,
and worked within the focused crate.

Two tool failures were real but recoverable:

- `function-call-db49a8cb-c710-48a9-8065-6ad360946107`: `list_dir` tried to
  read the campaign `prototype1` directory and was rejected as outside the
  configured roots.
- `function-call-761d4cff-37bb-476b-b8e6-fb528ada97ae`: `code_item_lookup`
  for `from_config` hit duplicate method matches in `manager.rs`.

The edit lifecycle then degraded. The model repeatedly patched the same file:

- `e9a1246c-c602-5b49-a52c-1eb1270c9384`: applied one edit to
  `manager.rs`.
- `9274214e-8ce1-5a06-8704-ba20bcaf7e0a`: applied another edit to
  `manager.rs`.
- `abf68a4f-efbc-5424-89d9-e1a481d28eaa`: first appeared as a proposal, then
  was rejected with "No non-semantic edits were applied".
- `a2b810b3-e8b0-5db8-bc7f-83debe15ed78`: applied another edit to
  `manager.rs`.
- `fc6e11d6-ad68-5302-8e98-fec82314799e`: applied another edit to
  `manager.rs`.

The final cargo failure shows that the model did use validation feedback, but
the attempt timed out before it repaired the remaining errors.

## Protocol Review And Blind Spots

The campaign protocol state is partial, and no child-attempt-specific protocol
review exists for r5. The available review surface is the compact headless-TUI
artifact plus git inspection.

Protocol or playback should not credit this as a successful child merely
because several proposals applied. The final state was timeout plus failed
focused compile plus absent submitted result.

## Positive Examples And Adjudication Candidates

Positive evidence:

- The model found a plausible hot path in MCP startup/health-check management.
- It ran cargo before and after edits, and the final failed cargo result was
  model-visible.
- It recovered from path-scope and duplicate-lookup failures instead of
  stopping immediately.

Candidate adjudication fields:

- Did the attempt leave a committed child artifact, or only a dirty checkout?
- Did final validation pass after the last applied edit?
- Did the model run the request contract or only focused crate checks?
- Did same-file edit churn produce stale or internally inconsistent code?
- Did performance edits weaken health checks without measurement?

## Classification

- `mechanical completion`: headless artifact present; attempt terminal
  `timed_out`.
- `benchmark outcome`: no admissible child result; submitted result absent;
  branch dirty and uncompilable.
- `provider/tool trace`: compact TUI trace present; full provider playback for
  the child attempt is not present.
- `recorded tool lifecycle`: several proposals applied, one proposal rejected,
  and two tool failures recorded.
- `information success`: moderate; localization to `manager.rs` was useful.
- `learning signal`: weak; cargo failure was visible, but no successful repair
  followed before timeout.
- `protocol judgment`: campaign protocol partial; no child-specific protocol
  judgment for r5.

## Action Items

1. Treat r5 as a failed broad-harness attempt, not as a candidate child.
2. Add an adjudication check for dirty timed-out workspaces with absent
   submitted-result files.
3. Add a validation-scope signal that distinguishes focused crate cargo from
   the request-declared validation contract.
4. Track same-file repeated edit churn and final compile failure as a negative
   child-attempt signal.
