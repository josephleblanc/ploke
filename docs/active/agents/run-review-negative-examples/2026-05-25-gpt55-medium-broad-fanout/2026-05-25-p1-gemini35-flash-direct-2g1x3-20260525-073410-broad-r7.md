# Prototype 1 Broad-Harness Attempt Review: `p1-gemini35-flash-direct-2g1x3-20260525-073410` r7

## Verdict

The r7 broad-harness attempt is a failed child attempt. It produced a
headless-TUI result artifact and a dirty checkout diff, but it timed out after
900 seconds, did not write the request-declared submitted result file, and did
not commit a candidate branch.

The attempted patch changed `ploke-records` string ID wrappers from `String` to
`ploke_core::ArcStr` and repaired two direct constructor sites. That is a
plausible allocation-reduction idea for heavily cloned record IDs, but the
attempt did not reach the request contract and ended with mixed validation:
one focused `ploke-records` test success, one focused feature-command failure,
one `ploke-eval` test failure, and one earlier focused compile failure that
was later partly repaired.

The r7 indexer shutdown panic should remain a separate investigation. It is
real log evidence, and r7 also has `ContentMismatch` warnings for
`selection.rs`, but the r7 terminal record itself is `timed_out`, not
`tool_failed`. I did not find evidence that the shutdown panic directly
overrode the terminal result recorded in the r7 headless artifact.

## Evidence Roots

- Campaign:
  `/home/brasides/.ploke-eval/campaigns/p1-gemini35-flash-direct-2g1x3-20260525-073410`
- Parent node: `node-b2a63be3e50c7ee8`
- Request slot:
  `prototype1/messages/edit-harness-request/node-b2a63be3e50c7ee8-r7.{json,md}`
- Headless result:
  `prototype1/messages/edit-harness-result/node-b2a63be3e50c7ee8-r7.headless-tui.json`
- Request-declared submitted result:
  `prototype1/messages/edit-harness-result/node-b2a63be3e50c7ee8-r7.json`
- Workspace:
  `prototype1/workspaces/edit-harness/node-b2a63be3e50c7ee8-r7`
- Branch:
  `prototype1-broad-broad-harness-request-node-b2a63be3e50c7ee8-r7`
- Parent commit:
  `bf8269e1511dfcb27c7b91dfca56ea290464413a`
- Eval log checked for r7 ContentMismatch and shutdown evidence:
  `/home/brasides/.ploke-eval/logs/ploke_eval_20260525_005146_1721114.log`

I did not advance the loop, rerun cargo in the candidate workspace, commit the
dirty branch, or update the run-review README. This review uses persisted
artifacts plus read-only git/log inspection.

## Execution Path

This is the broad-harness child attempt path:

```text
prototype1 child-plan broad-harness request
  -> tui_adapter::run_headless_with_model
  -> headless-TUI result artifact
  -> dirty attempt workspace
```

The request binding targeted artifact
`artifact:git-commit:bf8269e1511dfcb27c7b91dfca56ea290464413a` under policy
`workspace except ploke-eval`. The request asked for edits outside protected
core and listed the intended validation contract as:

```text
cargo check -p ploke-eval
cargo test -p ploke-eval edit_surface
```

The recorded validation list does not show that exact contract completing. The
closest broad check was `cargo test -p ploke-eval`, which failed.

## Record Inventory Classification

- `record present, manual join needed`: the request JSON, prompt markdown,
  headless result JSON, terminal object, validation list, compact TUI events,
  prompt diagnostics, attempt workspace, branch, and checkout diff all exist,
  but this review had to join them manually by request slot and workspace path.
- `record absent`: the request-declared submitted result
  `node-b2a63be3e50c7ee8-r7.json` was not written.
- `record present, playback gap`: the headless-TUI event stream exists with
  242 compact events, but it is not a full child-attempt playback surface with
  first-class ordered provider response, tool lifecycle, and checkout state
  iterators.
- `record present, manual join needed`: the r7 `ContentMismatch` and indexer
  shutdown evidence is in the process log, not joined directly into the r7
  headless result artifact.
- `operator/convenience record`: the checkout diff, branch/HEAD inspection,
  and process log line search are review evidence, not typed campaign
  authority records.

The runtime-playback inventory was checked before classifying the absent
submitted result. The inventory already distinguishes broad-harness state,
headless TUI sidecars, campaign closure, run-root evidence, protocol evidence,
and operator/convenience records.

## Closure And Campaign State

Campaign closure reports registry `complete`, eval `complete`, and protocol
`partial`. For the one instance, `BurntSushi__ripgrep-2209`, closure points to
the parent eval run root:

```text
/home/brasides/.ploke-eval/instances/prototype1/p1-gemini35-flash-direct-2g1x3-20260525-073410/BurntSushi__ripgrep-2209/runs/run-1779695507168-structured-current-policy-58118bd8
```

The protocol state is only intent-segmentation complete. Tool-call review and
segment review are missing in `closure-state.json`. That is campaign context,
not evidence that r7 produced an admissible child artifact.

I ran the skill trace audit against the parent eval run root as context. It
reported 93 provider responses, 92 provider-emitted tool calls, 92 recorded
tool calls, and no provider/recorded call-id mismatch. That audit is for the
parent benchmark eval run, not for the r7 broad-harness child attempt.

## Attempt Output

Terminal state:

```text
timed_out, secs = 900
```

Result artifact counts:

- 7 attempt records.
- 242 compact events.
- 32 `list_dir` requests.
- 70 `read_file` requests.
- 4 `request_code_context` requests.
- 4 cargo requests.
- 3 `apply_code_edit` requests.
- 2 `non_semantic_patch` requests.
- 1 `code_item_lookup` request.

The prompt diagnostics show context mode `Off`, workspace loaded, 27 members,
focused root `crates/ploke-records`, BM25 ready with 6512 docs, no included
RAG parts, and a 2048 max leased-token budget.

The request-declared submitted result path does not exist. The headless result
exists as `node-b2a63be3e50c7ee8-r7.headless-tui.json`, but that is the
diagnostic wrapper result, not the child-submitted result requested in the
contract.

## Checkout Diff

The final workspace branch is dirty and uncommitted:

```text
## prototype1-broad-broad-harness-request-node-b2a63be3e50c7ee8-r7
 M crates/ploke-records/src/history.rs
 M crates/ploke-records/src/ids.rs
 M crates/ploke-records/src/selection.rs
```

`HEAD` remains the parent commit
`bf8269e1511dfcb27c7b91dfca56ea290464413a`.

Tracked diff stat:

```text
crates/ploke-records/src/history.rs   |  2 +-
crates/ploke-records/src/ids.rs       | 14 +++++++++++++-
crates/ploke-records/src/selection.rs |  4 ++--
3 files changed, 16 insertions(+), 4 deletions(-)
```

Substantive change:

- `string_id!` now emits tuple wrappers around `ploke_core::ArcStr` instead of
  `String`.
- `string_id!` now adds `From<String>` and `From<&str>` impls for each wrapper.
- `ArtifactRefRecord::try_from` now constructs legacy artifact IDs with
  `ArtifactId::from(...)`.
- `MetricSet::absent_from_legacy_record` now constructs placeholder
  `HistoryHash` values with `HistoryHash::from(...)`.

The edit stayed outside the protected `ploke-eval` core surface.

## Validation

Recorded validations:

- `function-call-57ab10a2-84ea-426a-af22-58b4dbc95d84`: focused
  `cargo test`, manifest `crates/ploke-records/Cargo.toml`, success.
- `function-call-b08f5c56-416c-4c09-80e5-99cd43ae0221`: focused
  `cargo test --features all-features`, manifest
  `crates/ploke-records/Cargo.toml`, failed immediately because
  `ploke-records` does not contain a feature named `all-features`.
- `function-call-a429ce8b-49e4-4dad-8541-0f89edc37464`: workspace-scope
  `cargo test -p ploke-eval`, manifest workspace `Cargo.toml`, failed with
  exit code 101 and 486 warnings reported in the tool summary.
- `function-call-ea95f162-f2db-4cac-965c-6a9b67dbe662`: focused
  `cargo check`, manifest `crates/ploke-records/Cargo.toml`, failed with 3
  `E0308` mismatched-type errors after the first `ArcStr` edit. The model then
  applied follow-up repairs to `ids.rs`, `history.rs`, and `selection.rs`.

There is no recorded successful validation after the final `selection.rs`
patch. The attempt timed out before proving the final checkout state.

## Trace Reconstruction

The model started with a successful focused `cargo test` before editing, then
explored the workspace and broad Prototype 1 evidence. Early retrieval for
`descendant performance` and `performance` returned `PerformanceMetrics` in
`ploke-llm`, which was not a strong lead for improving this benchmark.

The model then spent substantial time reading `ploke-records`,
`successor_selection`, and `prototype1_state/history.rs`. Several `read_file`
calls returned empty content for high line ranges in very large files while
still reporting `ok:true`. This likely wasted context and time without giving
useful information.

The first concrete edit changed the `string_id!` macro to use `ArcStr`, but
did not add conversion helpers. The subsequent focused `cargo check` surfaced
three `E0308` errors where code still constructed wrappers directly from
`String`. The model repaired that by adding `From<String>` and `From<&str>` in
the macro and patching the direct `ArtifactId` and `HistoryHash` constructor
sites.

One semantic edit failed because the tool rejected
`canon=crate::history::try_from` as an invalid method target, then the same
call recorded an internal staging failure. The model recovered by using
`non_semantic_patch` for the `history.rs` repair. That recovery was useful, but
the attempt did not complete with final passing validation or a submitted
result.

## ContentMismatch And Indexer Shutdown

The r7 log contains real `ContentMismatch` warnings for
`crates/ploke-records/src/selection.rs`. The checked process log includes
warnings at lines `1082393`, `1082394`, `1082437`, `1082438`, `1082481`, and
`1082482` for `MetricComparison` and `ScoreChildPropRecord` using
`file_tracking_hash` `5175a11a-f7a1-5086-a9e4-fc37c8a3a527` in the r7
workspace. This is consistent with stale snippet/file-hash evidence after
candidate edits.

The same process log also contains the indexer shutdown panic cluster:

```text
759373: Cannot send shutdown message, other side dropped
759383: Sending Indexing Failed ... task 29749 panicked ... Failed to shutdown CallbackManager ...
854749: Sending Indexing Failed ... task 38553 panicked ... Failed to shutdown CallbackManager ...
854751: Sending Indexing Failed ... task 37429 panicked ... Failed to shutdown CallbackManager ...
```

That panic is operationally important but should not be collapsed into this
attempt review as the r7 terminal cause. The r7 headless result's terminal
record is explicit:

```text
{"terminal":"timed_out","secs":900}
```

So the right classification here is:

- `ContentMismatch`: present, manual log join needed; stale index/read evidence
  during post-edit indexing.
- indexer shutdown panic: present in the campaign process log; related
  operational failure already covered by the separate investigation.
- r7 terminal result: timeout in the headless artifact, not a direct
  shutdown-panic terminal record.

## Protocol Review And Blind Spots

The campaign protocol state is partial, and no child-attempt-specific protocol
review exists for r7. The available r7 review surface is the compact
headless-TUI artifact, the request/result files, git inspection, and process
log correlation.

Protocol or playback should not credit r7 because several edits applied. The
final state was timeout plus absent submitted result plus dirty uncommitted
checkout plus no final successful validation after the last edit.

The compact TUI result does preserve a useful staged-versus-applied lifecycle
signal: `apply_code_edit` completions first reported `staged:1, applied:0`,
then later approval/apply records reported `applied:1`. Any future playback or
protocol judgment must keep those phases separate.

## Positive Examples And Adjudication Candidates

Positive evidence:

- The model identified a plausible allocation surface: heavily cloned passive
  record ID wrappers.
- The model used compiler feedback to repair direct `String` constructor
  breakage after the `ArcStr` change.
- The model recovered from an invalid semantic-edit target by switching to a
  non-semantic patch.

Candidate adjudication fields:

- Did final validation pass after the last applied edit?
- Did the attempt write the request-declared submitted result?
- Did the model run the request-declared validation contract, or only adjacent
  focused checks?
- Did `ok:true` empty/truncated reads consume time without useful evidence?
- Did a timeout preserve applied edits but leave no admissible child artifact?
- Did `ContentMismatch` appear after same-file edits, and was it terminal or
  only diagnostic noise?

## Classification

- `mechanical completion`: headless artifact present; terminal `timed_out`.
- `benchmark outcome`: no admissible child result; submitted result absent;
  branch dirty and uncommitted.
- `provider/tool trace`: compact TUI trace present; no full child-attempt
  provider playback surface joined to the attempt.
- `recorded tool lifecycle`: four applied proposals, three failed tool-result
  records, and four validation records.
- `information success`: moderate; the final patch is coherent, but the path
  there included weak retrieval and empty reads.
- `learning signal`: moderate; compiler diagnostics led to targeted repairs.
- `protocol judgment`: no child-specific protocol judgment; campaign protocol
  is partial.

## Action Items

1. Treat r7 as a failed broad-harness attempt, not as a candidate child.
2. Keep the indexer shutdown panic in its dedicated investigation and repair
   lane; do not use it as the terminal explanation for r7 unless a future
   typed record joins it directly to this attempt's terminal transition.
3. Add adjudication for dirty timed-out workspaces with absent submitted-result
   files.
4. Add a validation-scope signal for attempts that run adjacent focused checks
   but not the request-declared validation contract.
5. Preserve `ContentMismatch` as a post-edit index/read consistency signal
   without automatically treating it as terminal failure.
