# Ploke-loop source inventory: implementation invariants slice

Date: 2026-06-19
Task: `t_34283835`
Repo observed: branch `prototype1-parent-mwv-live-r16-dangling-symlink-surface-fix-20260615t003337z-gen0`, HEAD `2f50fdcc`
Status: source-inventory slice for publication/docs/proof synthesis; not a proof of the architecture.

## Scope and caveats

This slice starts from the source corpus named in parent card `t_d6ddab0d` and adds implementation-grounded anchors from current VM code. It is biased toward material that later paper, mdBook, and symbolic-proof tracks can cite without relying on memory.

Important caveat: `t_d6ddab0d` says typestate transitions are complete on the user's home machine but not yet pushed. In this VM checkout, `crates/ploke-eval/src/cli/prototype1_state/typestate/` is absent, and the current top-level module docs still say the typed states are only partially wired into the live controller. Treat current VM typestate evidence as implementation-grounded but incomplete, not final source of truth.

Classifications used here:

- `canonical/current`: current implementation or current mdBook/evalnomicon page that explicitly points to current implementation.
- `implementation-grounded`: source/test/doc is tied to current code paths but may be partial, transitional, or read-side only.
- `stale-but-useful`: useful context or target language whose observed commit/path/status is behind current VM HEAD or explicitly draft/historical.
- `speculative`: formal target/proposal not implemented as such.
- `duplicate`: overlaps a stronger source and should be cited only for wording/background.
- `missing due to home-machine typestate changes not yet pushed`: expected material named by the parent card but absent in this VM checkout.

## Search method and terms

Tool-backed searches performed:

- `kanban_show(t_34283835)` and `kanban_show(t_d6ddab0d)` for task scope and parent corpus.
- `search_files(target="files", pattern="*.rs", path="crates/ploke-eval/src/cli/prototype1_state")` to enumerate current Prototype 1 state modules.
- `search_files(target="files", pattern="*.md", path="docs/workflow/evalnomicon")`, plus focused `docs/active/agents/2026-06-{02,06,09,19}_...` scans, for the parent-card documentation corpus.
- `search_files(output_mode="files_only", file_glob="*.rs", path="crates", pattern="schema|relations|Cozo|embedding|GraphRAG|graphrag|config|event|invariant|typestate|parent|successor|handoff|History|Surface|Crown")` for implementation surfaces.
- `search_files(output_mode="files_only", file_glob="*.md", path="docs", pattern="schema|database|Cozo|immutable|mutable|invariant|typestate|graph|embedding|event|config|parent|successor|handoff")` for docs surfaces.
- Focused Python term scans over `crates/{ploke-eval,ploke-tree,ploke-records,ploke-db,ploke-rag,ploke-embed,ploke-tui,ploke-core}/src/**/*.rs` and the parent-card doc directories using terms: `schema`, `database`, `Cozo`, `immutable`, `mutable`, `invariant`, `typestate`, `graph`, `embedding`, `event`, `config`, `parent`, `successor`, `handoff`, `History`, `Surface`, `Crown`, `authority`, `channel`.
- `search_files(target="files", pattern="typestate", path="crates/ploke-eval/src/cli/prototype1_state")` returned no files.

## Inventory: Prototype 1 authority, History, Crown, and handoff

| Source | Anchor | Classification | Annotation |
| --- | --- | --- | --- |
| `crates/ploke-eval/src/cli/prototype1_state/mod.rs` | module docs, lines 1-80 | canonical/current | Defines the main vocabulary for Configuration, Artifact, Runtime, Journal, History, and Crown. It also states design constraints: no global active Parent singleton, no overwriting eval state, no self-report promotion, no coupling worktree layout to semantic tree, and no global authority implied by successor authority. |
| `crates/ploke-eval/src/cli/prototype1_state/history/mod.rs` | module docs, lines 1-120 | canonical/current | The strongest current source for History/Crown invariants. It states that scheduler, branch registry, CLI reports, previews, dashboards, and database side tables are projections/caches/evidence, not sealed History authority; it also records the intended startup-to-Parent authority sequence and the cross-runtime handoff contract. |
| `crates/ploke-eval/src/cli/prototype1_state/history/seal/mod.rs` | `Entry<S>`, `Entry<Admitted>::entry_hash`, `OpenBlock`, lines 108-269 | canonical/current | Implements typed History entries and open block fields. The `OpenBlock` comment is a useful proof-gap anchor because it names what is implemented now and what is not: global append position, Merkle/authenticated lineage-head map proofs, artifact manifest digest commitments, and uniform typed startup/admission carriers. |
| `crates/ploke-eval/src/cli/prototype1_state/parent.rs` | `Unchecked`, `Checked`, `Ready`, `Startup<S>`, `Parent<S>`, lines 32-101 | canonical/current | Current parent role typestate carriers. `Startup<Validated>` is described as the local single-ruler startup gate, and fields are private so transport evidence such as successor invocation JSON cannot be converted into Parent readiness by convention. |
| `crates/ploke-eval/src/cli/prototype1_state/successor.rs` | `State` and `Record`, lines 1-84 | implementation-grounded | Successor is not a separate live controller role; it is the incoming Parent before handoff acknowledgement. The durable transition states (`Selected`, `Spawned`, `Ready`, `TimedOut`, `Completed`, etc.) are citeable for handoff evidence but not sufficient by themselves to prove authority. |
| `crates/ploke-eval/src/cli/prototype1_state/invocation.rs` | module docs and `Role`, lines 1-25 and 96-140 | canonical/current | Defines the persisted bootstrap contract for runtime attempts. It explicitly separates `Child` as a leaf evaluator from `Successor` as a handoff token and warns against drifting into a generic fresh-binary-can-do-anything surface. |
| `crates/ploke-eval/src/cli/prototype1_state/channel.rs` | module docs, message/stream markers, `CanSend`/`CanRecv`, lines 1-120 | implementation-grounded | Role-indexed parent/child runtime channel scaffold. The file is staged (`allow(dead_code)`), but the design is implementation-grounded: Parent/Child role-state carriers serve as authority tokens while transport is parameterized. |
| `crates/ploke-eval/src/cli/prototype1_state/authority.rs` | module docs and `AuthorityError`, lines 1-21 and 29-64 | stale-but-useful | Explicitly marked `REMOVE BY 2026-04-28` and not fully wired, but still valuable for authority vocabulary: ActiveRoot is not parent authority, ChildRoot is not active-checkout mutation permission, and successor bootstrap must validate active root/binary relation before parent authority. |
| `crates/ploke-eval/src/cli/prototype1_state/run/mod.rs` | module docs, lines 1-10 from search hit | implementation-grounded | Landing area for live parent execution path; useful as a routing anchor after the high-level invariant docs. Re-check this file before using any prose that claims the exact current order of runtime operations. |
| `crates/ploke-eval/src/cli/prototype1_state/c4.rs` | `ObservedChild`, lines 43-48 from search hit | implementation-grounded | Names parent-side observation of child completion after `C4`. Useful for the symbolic track's parent-observes-child-before-selection proposition, but less authoritative than `parent.rs`, `successor.rs`, and History sealing. |

## Inventory: edit surface, immutable/mutable surface, evidence, and admission

| Source | Anchor | Classification | Annotation |
| --- | --- | --- | --- |
| `crates/ploke-eval/src/cli/prototype1_state/edit_surface/tui.rs` | module docs and `GeneratorSurfaceVersion`, lines 1-10 and 140-162 | canonical/current | Eval-owned adapter boundary for TUI edit proposals and DB-resolved material spans. It says lower proposal/request/material shapes do not gain authority; eval binds projections to an Artifact, checks rule/bounds digests, converts material spans after hash validation, and admits apply evidence only as all-applied or rejected. |
| `crates/ploke-eval/src/cli/prototype1_state/backend/surface_admission.rs` | `GitWorktreeBackend::validate_edit_surface_candidate`, lines 20-170 | canonical/current | Current hard admission path for checked edit carriers. It validates nonempty/single-file touches, normal repo-relative paths, path membership in surface policy, expected file hashes, span bounds, generator-surface equality, and then builds checked graph/tui surface evidence. |
| `crates/ploke-eval/src/cli/prototype1_state/edit_surface/harness_request.rs` | `BroadHarnessRequest`, `RequestAdmissionBinding`, lines 16-41 from search hit | implementation-grounded | Shows the current request/admission binding seam between broad harness requests and `SurfacePolicyId`. Useful for mdBook diagrams of request-bound planning, but the active carrier aliases indicate recent migration/compatibility pressure. |
| `crates/ploke-eval/src/cli/prototype1_state/evidence.rs` | module docs, lines 1-54 | canonical/current | The child-evidence grouping invariant is explicit and conservative: persisted evidence crossing the boundary must deserialize as typed `Stored<T>` records; loose JSON/path inference must not feed child evidence, metrics, selection, scoring, or authority semantics. |
| `crates/ploke-eval/src/cli/prototype1_state/evidence_inventory.rs` | module docs, `HistoryCommitmentLane`, `InventoryRow`, lines 1-95 | canonical/current | This is the best current schema for documentation/publication evidence-surface inventory. It distinguishes projection-only, sealed History, and ingress-that-may-seal lanes; it also records producers, consumers, provenance keys, and authority treatment. |
| `crates/ploke-eval/src/cli/prototype1_state/profile.rs` | `AntiAttractorPolicy`, lines 389-408 from search hit | implementation-grounded | Captures run-profile policy for surface/skeleton attractors and surface-freshness bias. Cite as config/policy evidence, not authority. |
| `docs/workflow/evalnomicon/src/prototype1/edit-surface.md` | `Surface partition`, lines 1-10 | canonical/current | Short collaborator-facing explanation of bounded edit surface: `ArtifactSurface = Immutable + Mutated + Ambient`. Cite with current code anchors above for the implementation-backed version. |
| `docs/workflow/evalnomicon/src/prototype1/persistence-and-observability.md` | `Evidence families` and `Projection warning`, lines 12-21 | canonical/current | Concise wording for evidence families and projection warnings: scheduler is mutable legacy projection; branches preserve evidence but are not Crown authority. Pair with `evidence_inventory.rs` for schema detail. |

## Inventory: passive records, read-side graph, playback, and proof facts

| Source | Anchor | Classification | Annotation |
| --- | --- | --- | --- |
| `crates/ploke-records/src/history.rs` | module docs, lines 1-6 | canonical/current | The passive record mirror boundary is explicit: `Block<S>` and `Entry<S>` typestate remain in `ploke-eval`; records here mirror persisted sealed/admitted shapes for projection/UI and do not prove hashes, authorize successor startup, or advance lineage heads. |
| `crates/ploke-records/src/proof_facts.rs` | module docs and proof IDs, lines 1-14 and 37-95 | canonical/current | Stable DTO vocabulary for proof-grade call/effect graph extraction. The module intentionally does not validate proofs, admit History, grant Crown authority, or execute build/proc-macro code; it provides BuildDomain, ExpansionBoundary, CallSite, Definition, EffectSeed, AuthorityFact, and Blocker identities. |
| `crates/ploke-records/src/proof_authority.rs` | module docs and `AuthorityExtractionConfig`, lines 1-7 and 45-70 | implementation-grounded | Conservative source extractor for authority-shaped calls. It recognizes explicit authority boundary calls but requires opt-in admitted sites; exact-but-untrusted or malformed authority-like calls remain blocked. |
| `crates/ploke-db/src/proof_graph.rs` | `ProofGraphStore`, `ProofInvariantStatus`, schema creation, lines 10-120 | canonical/current | Database/query surface for proof-useful facts and active invariant checker findings. It is the natural bridge between proof DTOs and GraphRAG/symbolic-proof consumers, with blocker/provenance rows retained rather than erased. |
| `crates/ploke-tree/src/browser.rs` | module docs, `RunExecutionGraph`, `ExecutionNodeKind`, `ExecutionEdgeKind`, lines 1-6 and 35-103 | implementation-grounded | Renderer-neutral browsing boundary: it does not parse CLI output, read run dirs, or decide History authority. It shapes typed records/projections into runtime/artifact/operation/evaluation/selection/handoff graph nodes and edges. |
| `crates/ploke-tree/src/graph/artifact_tree.rs` | `Tree`, `Marks`, `Diagnostics`, lines 14-27 and 71-101 | implementation-grounded | Artifact-first projection over a `Graph`. The projection owns traversal indexes/diagnostics while borrowing artifact facts and relation sources; `Marks` link current archaeology/proof docs for lineage highlighting and unconsidered-child evidence. |
| `crates/ploke-tree/src/tests.rs` | `scheduler_nodes_become_mutable_projection_tree_nodes`, `parent_child_relationships_use_only_explicit_scheduler_parent_fields`, lines 142-190 | canonical/current | Tests encode important projection invariants: scheduler nodes are mutable projections, and parent/child relationships use explicit scheduler parent fields rather than branch-only or incidental graph shape. Useful for symbolic proof examples because they are executable assertions. |
| `crates/ploke-tree/src/tests.rs` | ignored real-run diagnostic `real_run_run_forest_node_ids_are_not_artifact_tree_ids`, lines 31-136 | implementation-grounded | Real-run diagnostic, ignored because it reads a user-local run root. Useful as evidence of a known naming/identity pitfall: RunForest node IDs and ArtifactTree artifact IDs are different and only carried artifact refs should bridge them. |

## Inventory: schema/database, embeddings, GraphRAG, config, and event surfaces

| Source | Anchor | Classification | Annotation |
| --- | --- | --- | --- |
| `crates/ploke-db/src/database.rs` | `DEFAULT_EMBEDDING_SET`, `TypeRelationImportMode`, `is_typed_type_graph_relation`, lines 35-70 | canonical/current | Core Cozo database wrapper and schema import surface. The typed type graph relation whitelist is a useful current anchor for mdBook schema/database discussion and for proof claims that must account for relation-level schema drift. |
| `crates/ploke-db/src/multi_embedding/schema.rs` | `EmbeddingVector`, `script_create_from_set`, `validate_embedding_vec`, lines 14-82 | canonical/current | Current embedding vector schema creates Cozo vectors as `<F32; dims>` while Rust stores input vectors as `Vec<f64>`. This is a citeable embedding/storage invariant; note that readback is Cozo vector-valued, not list-valued. |
| `crates/ploke-rag/src/core/mod.rs` | module docs, `RagConfig`, `TypeContextConfig`, lines 1-9 and 61-115 | canonical/current | Current RAG orchestration surface for sparse/dense/hybrid retrieval and token-budgeted context assembly. Type-context expansion is feature-gated and configured separately, so docs should distinguish GraphRAG/type-neighbor claims from generic RAG. |
| `crates/ploke-tui/src/lib.rs` | module dataflow overview and subsystem list, lines 1-13 | canonical/current | Release-facing TUI bootstrap overview: config load, command routing, model/provider updates, config persistence, and subsystem startup (state manager, LLM manager, EventBus, Observability, FileManager, Indexer, optional RAG). |
| `crates/ploke-tui/src/user_config.rs` | module docs and config types, lines 1-3 and 35-120 | implementation-grounded | Current user configuration boundary for persistence, embedding/editing, router defaults, message verbosity profiles, and workspace registry env. Cite for collaborator docs on config/state persistence, not for Prototype 1 authority. |
| `crates/ploke-tui/src/app/mod.rs` | `App` channel fields, lines 135-139 from search hit | implementation-grounded | TUI runtime event/channel surface. Useful for user-event path diagrams, especially command-to-state-manager and broadcasted application event paths. |
| `crates/ploke-tui/src/chat_history.rs` | `UpdateFailedEvent`, hierarchical message docs, lines 168-192 from search hit | implementation-grounded | Current chat history has parent/child message semantics and validation-failure events. Useful for mdBook lifetime-of-user-message diagrams; separate from Prototype 1 parent/successor authority vocabulary. |
| `crates/ploke-tui/src/tools/request_code_context.rs` | request schema and type-context degraded notes, lines 9-84 from search hit | implementation-grounded | Tool-call surface for code graph retrieval. Important collaborator-doc anchor because the user-facing schema says good queries include identifiers, module names, file names, type names, or concise code terms, and it warns when typed-graph neighbors are unavailable. |

## Inventory: source corpus docs from `t_d6ddab0d`

| Source | Anchor | Classification | Annotation |
| --- | --- | --- | --- |
| `docs/workflow/evalnomicon/src/prototype1/index.md` | `Source priority`, lines 7-22 | canonical/current | Index explicitly prioritizes current code comments in `prototype1_state/mod.rs` and History code over older notes. Use as the citation policy for paper/mdBook tracks. |
| `docs/workflow/evalnomicon/src/prototype1/invariant-ledger.md` | `History is authority`, lines 14-23 | canonical/current | Clean human-readable invariant ledger: History is authority; scheduler/report/registry are projections. Cite after checking any nearby code changes, but this is aligned with current module docs. |
| `docs/workflow/evalnomicon/src/prototype1/history-crown.md` | `History` section, lines 1-7 | canonical/current | Compact explanation that History/Crown define a local authority boundary, not distributed consensus or whole-environment trust. Good for paper introduction/limitations. |
| `docs/workflow/evalnomicon/src/prototype1/runtime-authority.md` | `Authority shape`, lines 1-7 | canonical/current | Human-readable authority model: role-scoped, state-scoped, lineage-scoped. Pair with `parent.rs`, `invocation.rs`, and `history/mod.rs` for implementation backing. |
| `docs/workflow/evalnomicon/src/prototype1/runtime-loop.md` | current loop shape, lines 5-17 | canonical/current | Explains why changing an artifact does not change an already-running parent binary; descendants must be built into descendant runtimes before their behavior can be evaluated. |
| `docs/workflow/evalnomicon/src/prototype1/selection-and-evaluation.md` | selection boundary, lines 3-15 | canonical/current | Selection decides which child artifact/runtime may become successor; child self-evaluation is evidence, promotion requires parent-side policy plus History/Crown admission checks. |
| `docs/workflow/evalnomicon/src/prototype1/artifact-runtime-model.md` | core terms and graph views, lines 3-13 | canonical/current | Defines Artifact, Runtime, Lineage, and the need for multiple graph views. Use as an approachable entry point before code-level sources. |
| `docs/workflow/evalnomicon/drafts/runtime/parent-child-channel.md` | opening lines 1-7 | implementation-grounded | Runtime-channel plan aligns with `channel.rs`: replace scattered file protocol with a role-indexed channel contract. Treat as design direction, not proof of full live migration. |
| `docs/workflow/evalnomicon/drafts/runtime/authority.md` | opening lines 1-19 | implementation-grounded | Draft authority language names `ExecutionPath<Role, State> + Surface<Role> + Transport`. Useful for symbolic vocabulary, but subordinate to current code and `src/prototype1` pages. |
| `docs/workflow/evalnomicon/drafts/formal/detached-process-callgraph-proof-target.md` | purpose/source anchors, lines 7-18 | speculative | Target statement for detached-process and Crown safety proof. Valuable for proof goals but explicitly says it is not an implementation slice. |
| `docs/workflow/evalnomicon/drafts/formal/callgraph-implementation-design-for-detached-process-proof.md` | purpose/relationship, lines 7-19 | speculative | Designs the call-graph implementation target for proving no process outlives a runtime except through admitted handoff. Cite as target architecture, not current enforcement. |
| `docs/workflow/evalnomicon/drafts/formal/rustc-macro-expansion-backend-plan.md` | thesis and build-domain notes, lines 17-32 | speculative | Useful for macro/build.rs proof-gap language: raw expanded source should not become the canonical graph; BuildDomain and ExpansionProvenance should be first-class. Not currently implemented. |
| `docs/active/agents/2026-06-19_detached-process-crown-proof-spine/traceability.md` | traceability matrix, lines 21-80 | stale-but-useful | Very relevant proof-spine traceability, but observed HEAD in the file is `55062d4c` while this inventory observed `2f50fdcc`. Re-check before citing as current; useful for extension points, blocker vocabulary, and proof-consumer plan. |
| `docs/active/agents/2026-06-02_prototype1-state-loop-walkthrough/README.md` | purpose and reading notes, lines 13-21 | implementation-grounded | Developer-oriented walkthrough of `loop prototype1-state` as a typed runtime for one parent turn. Good mdBook seed; revalidate exact command flow against current `run/` and `cli_facing.rs`. |
| `docs/active/agents/2026-06-02_prototype1-state-loop-walkthrough/terminology-conflicts.md` | Node and parent-id conflicts, lines 13-38 | implementation-grounded | Important collaborator-doc cleanup source: distinguish syntax/code graph nodes from scheduler/runtime nodes, and distinguish parent id, parent node id, and node id. |
| `docs/active/agents/2026-06-02_prototype1-state-loop-walkthrough/campaign-configs.md` | purpose and config planes, lines 10-22 | implementation-grounded | Good config/state persistence companion for campaign/run-profile fields. Treat machine-local model examples as environment-specific, not canonical project state. |
| `docs/active/agents/2026-06-06_parent-successor-handoff-regression/README.md` | conclusion, lines 13-22 | stale-but-useful | Regression investigation identifies a broad batch finalization bug around provider/database fatal slots and parent child-plan/successor handoff writing. Useful historical evidence, but verify current code before using as live bug status. |
| `docs/active/agents/2026-06-09_prototype1-state-api-surface-migration/migration.md` | downstream consumer map, lines 1-12 | implementation-grounded | Maps DTO/graph import coupling across `ploke-records` -> `ploke-tree::FsRunStore` -> `Graph::from_records` -> `ploke-egui`. Useful for schema migration and read-side projection docs. |
| `docs/book/src/architecture/code-understanding-pipeline.md` | parse/transform/store/embed/index, lines 5-19 | canonical/current | Concise collaborator-facing pipeline for parsing -> graph rows -> Cozo -> embedding/indexing. Use with code anchors in `ploke-db`, `ploke-rag`, and parser/transform docs. |
| `docs/book/src/architecture/eval-and-projection-plane.md` | authority rule, lines 17-19 | canonical/current | Clear public-doc wording: passive records are evidence, not authority, and loading into projection must not imply permission to advance scheduler state, mutate History, or apply patches. |
| `docs/book/src/crate-guide/ploke-tree.md` | responsibilities, lines 6-13 | canonical/current | Read-side projection summary: `ploke-tree` turns records into RunForest, history-spined Graph, playback, and artifact-tree views; sealed History is primary lineage authority. |
| `docs/book/src/crate-guide/ploke-db.md` | responsibilities, lines 6-13 | canonical/current | Cozo-backed graph/retrieval DB responsibilities: schema initialization, transformed code graph storage, active embedding-set metadata, vector relations, HNSW search, and typed embedding data. |
| `docs/book/src/crate-guide/ploke-embed.md` | responsibilities, lines 6-13 | canonical/current | Embedding runtime responsibilities: backend abstraction, active embedding-set activation, vector relation updates, batching/cancel/dispatch. |
| `docs/book/src/crate-guide/ploke-rag.md` | internal structure, line 34 | canonical/current | RAG service composition: Database, EmbeddingRuntime, BM25, config, optional I/O. Use as high-level docs, not as a replacement for `ploke-rag/src/core/mod.rs`. |

## Likely stale, duplicate, or missing references

- Missing on this VM: `crates/ploke-eval/src/cli/prototype1_state/typestate/**` named in `t_d6ddab0d`; `search_files(..., pattern="typestate")` under `prototype1_state` returned zero files. This is likely the home-machine typestate work mentioned by the parent card.
- Current VM code is still transitional: `prototype1_state/mod.rs` says typed states are only partially wired, `channel.rs` is staged with `allow(dead_code)`, and `authority.rs` is explicitly stale by date. Do not treat those as full enforcement of the future typestate architecture.
- `docs/active/agents/2026-06-19_detached-process-crown-proof-spine/traceability.md` is highly relevant but observed a different HEAD (`55062d4c`) than the current VM HEAD (`2f50fdcc`), so cite it as stale-but-useful until revalidated.
- Older evalnomicon drafts in `docs/workflow/evalnomicon/drafts/runtime` and `docs/workflow/evalnomicon/drafts/formal` often duplicate concepts now expressed more safely in `docs/workflow/evalnomicon/src/prototype1/*` and code comments. Prefer the `src/prototype1` pages plus code anchors for canonical wording; use drafts for proof target/gap vocabulary.
- Several runtime walkthroughs contain machine-local paths or model selections (`/home/brasides/...`, local campaign IDs, local model files). Those are implementation-grounded examples but should not be published as general setup requirements.

## Citation priority for next tracks

1. For authority/History/Crown claims: cite `crates/ploke-eval/src/cli/prototype1_state/history/mod.rs`, `history/seal/mod.rs`, `parent.rs`, `invocation.rs`, and `docs/workflow/evalnomicon/src/prototype1/{invariant-ledger,history-crown,runtime-authority}.md`.
2. For immutable/mutable surface claims: cite `edit_surface/tui.rs`, `backend/surface_admission.rs`, `evidence.rs`, `evidence_inventory.rs`, and `docs/workflow/evalnomicon/src/prototype1/edit-surface.md`.
3. For read-side graph/projection claims: cite `crates/ploke-records/src/history.rs`, `crates/ploke-tree/src/browser.rs`, `crates/ploke-tree/src/graph/artifact_tree.rs`, and projection tests in `crates/ploke-tree/src/tests.rs`.
4. For proof/callgraph claims: cite `crates/ploke-records/src/proof_facts.rs`, `proof_authority.rs`, `crates/ploke-db/src/proof_graph.rs`, and treat formal detached-process docs as target/gap sources.
5. For collaborator pipeline docs: cite `docs/book/src/architecture/code-understanding-pipeline.md`, `crates/ploke-db/src/multi_embedding/schema.rs`, `crates/ploke-rag/src/core/mod.rs`, `crates/ploke-tui/src/lib.rs`, and the relevant crate-guide pages.
