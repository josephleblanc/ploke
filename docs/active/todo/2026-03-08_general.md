# General todo for March 8, 2026

## Possible issues

ploke-tui: Potentially lossy-by-design spot

  - run_event_bus emits EventBusStarted immediately at startup in event_bus/mod.rs:63.
  - App::new subscribes later in startup order (lib.rs:279 then lib.rs:286, subscription in app/
    mod.rs:186).
  - observability is spawned and subscribes inside its async function (observability.rs:51), so
    very early events can be missed before that task starts.
  - In practice this seems low risk because EventBusStarted is mostly readiness/diagnostic and not
    core workflow state.
