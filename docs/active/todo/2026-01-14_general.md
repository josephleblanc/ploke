# General todo

## Clean up complex types

- [ ] complex return type: turn into its own type

See `file_completion_suggestions` in `crates/ploke-tui/src/app/mod.rs:551`

- [ ] check for complex types pointed out by `clippy::type_complexity`

Create list of complex types which should be turned into their own structs.

## Clean up sloppy `default` usage

- [ ] audit usage of `unwrap_or_default`

The `unwrap_or_default` method can be used to introduce a lot of confusing
logical errors that bypass type checking. Identify all areas where this method
is being used. Later on we will want to rewrite these areas to either
explicitly return the default or use a functional approach to appropriately
handle the inner type.

The list of areas using `unwrap_or_default` can be placed here.
