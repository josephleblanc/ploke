# todo Apr 1, 2026

## UX fixes

Some annoying things that need fixes/improvement:
- Change default settings/visibility/verbosity:
  - Tool calls so not need to show their results with linebreaks by default.
  Experiment with layout per-tool and fit into one line.
    - Nice-to-have: clean array/vec for results (e.g. searched/found/etc)
    for easy reordering/experimentation with UI
  - context management: change to off by default to not surprise new users
  - verbosity issue: don't truncate model responses aimed primarily at user. 
    - do truncate reasoning/user-facing messages attached to tool calls,
    variable on verbosity level 
      - min = only tool name 
      - default = truncate after second line + dim
      - max = don't truncate + don't dim)
  - Change search + context builder to be hybrid (dense and sparse) by default
  - ContextPlan overlay: too verbose by default, needs tightening
    - concise 1-line summary, also build 2-line summary, test UX
    - expanding list issue: using `s` to select toggles showing code, which is
    fine, but enclosing collapsable item doesn't collapse afterwards when
    snippet is visible.
      - instead: make snippet expansion with `s` distinct from parent item expansion.
      - exception: if snippet shown and parent collapsed, command to collapse
      parent `h`/`LeftArrow` collapses snippet
      - exception: if snippet not shown and parent fully expanded, command to
      expand parent  `l`/`RightArrow` expands snippet
- Buggy/weird UI
  - sometimes lists allow selected item to move beyond visible list items, may
  be related to assumed terminal height/width. Should never happen, add strict
  testing to confirm tests.
- Add config for styling/colorscheme
  - UI coloring + syntax highlighting is bad
  - a config would allow fast iteration
  - make it dynamically loaded
  - .md syntax highlighting in chat (background color highlighting now)
- UX testing/workflow
  - Align code with this clearly defined policy on save/load/index/update
  behavior in
  `active/agents/2026-04-01_ux-polish/2026-04-01_indexing-policy.md`
- Needs assessment (may require larger change): perform bm25 indexing first,
  start background process for embedding


## UX Debugging + Evals Features

- Add `Recording` mode to persist chat with extra metadata
  - Add `+`/`-` hotkey in `Normal` mode for good/bad indicator on AI response
    - This can be a good way to get an early idea of tool use correlation with good output
