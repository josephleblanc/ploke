# Archaeology Index

| archaeology id | domain claim | chosen primary carrier | status | UI fields | `ploke-records` | `ploke-tree` | `ploke-eval` | `ploke-tui` | `ploke-llm` | report |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `artifact-identity` | identify an Artifact in inspector surfaces without collapsing identity, handle, and provenance | `ArtifactId` as primary UI handle; `ArtifactRef` as alias; `TreeKeyHash` as secondary checkout identity carried on `ArtifactNode.ids.tree_keys` when sealed History provides an artifact claim | `draft` | `render_artifact_identity`, `render_artifact_ids_for_inspector`, run-forest `base artifact` / `derived artifact` rows, CLI `artifact_ids_report` | `ids::ArtifactId`, `history::ArtifactRefRecord`, `history::TreeKeyHashRecord`, `history::ClaimsRecord.artifact` | `graph::ArtifactIdentity`, `graph::ArtifactIds`, `graph::ArtifactNode`, `graph::OpeningAuthorityNode`, artifact-tree grouping sources | `loop_graph::ArtifactId`, `Prototype1NodeRecord.base_artifact_id`, `Prototype1NodeRecord.derived_artifact_id`, `selection::artifact_ref`, `history::ArtifactSurface.tree_key` | none yet | none | [`ploke-tree-graph/artifact-identity.md`](ploke-tree-graph/artifact-identity.md) |
| `artifact-relations` | render default artifact-canvas edges from typed graph relations with visible `P_H ∪ P_C`, while `P_O` and `P_B` remain graph-owned context/provenance inventory | `ploke_tree::graph::artifact_tree::{HistoryEdge, ProducedChildEdge}` carried by `Tree<'g>` relation families | `draft` | `project_artifact_tree`, default artifact canvas edge rendering, contract/edge diagnostics | `history::SealedBlockRecord.state.header.{opened_from_artifact,active_artifact,selected_successor.artifact}`, `child_plan::ChildPlanRecord`, `child_plan::ChildPlanChildRecord`, candidate branch artifact ids | `graph::artifact_tree::Tree::{history_successors,produced_child_edges,opened_from_edges,applied_patch_edges}` | History sealing and parent-published child planning mint the visible/default relations; candidate/evaluation records still mint `P_B` provenance | none yet | none | [`ploke-tree-graph/artifact-relations.md`](ploke-tree-graph/artifact-relations.md) |
| `artifact-promotion-continuity` | default artifact-tree should show the selected child Artifact and later next-parent base Artifact as one displayed node when graph-owned child-plan continuity proves they are the same artifact for this view, while generation-1 sibling children remain attached to their initial parent instead of becoming singleton roots | graph-owned promotion alias map in `ploke_tree::graph::artifact_tree::promotion_aliases`, with parent endpoint materialization governed by `material_keys` and `parent_artifact_key` | `draft` | default artifact canvas node grouping, artifact inspector continuity semantics, CLI edge/component diagnostics | `child_plan::ChildPlanRecord.parent_node_id`, `scheduler::{NodeRecord,RunnerRequestRecord}.{base_artifact_id,derived_artifact_id}` | `graph::ParentCreateAttempt::{derived_artifact_id,next_parent_base_artifact_id}`, `graph::artifact_tree::{promotion_aliases,canonical_key,material_keys,parent_artifact_key}` | child-plan publication and later parent-owned child planning mint the continuity facts | none yet | none | [`ploke-tree-graph/artifact-promotion-continuity.md`](ploke-tree-graph/artifact-promotion-continuity.md) |
