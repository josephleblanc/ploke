# Developer Guide

This section records repeatable contributor workflows: building, checking, testing, fixture setup, and documentation maintenance.

Follow the repository `AGENTS.md` for agent-specific guardrails and the nearest scoped `AGENTS.md` when working inside a crate.
