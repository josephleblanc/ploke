Link: docs/plans/agentic-system-plan/impl-plan/plan_20250831-215141Z.md

Summary
- Added live test matrix `crates/ploke-tui/tests/live_openrouter_matrix.rs` to iterate models/prompts/tool_choice and collect latency + evidence.
- Added writer test `write_tool_capable_models_to_ai_temp` to save tool-capable models into `crates/ploke-tui/ai_temp_data/`.
- Hardened existing live test `live_openrouter_tools.rs` to skip gracefully when no tools-capable endpoints are available for the chosen model.

Artifacts
- Tool-capable models:
  - JSON: crates/ploke-tui/ai_temp_data/tool_capable_models.json
  - Text: crates/ploke-tui/ai_temp_data/tool_capable_models.txt
- Live matrix run outputs (latest):
  - Directory: crates/ploke-tui/target/test-output/openrouter_matrix/run-YYYYMMDD-HHMMSS/
  - Metrics: metrics.jsonl
  - Per-combo request/response snapshots under subfolders.

Evidence (live)
- Ran: cargo test -p ploke-tui --test live_openrouter_matrix --features live_api_tests -- --nocapture
  - Result: 1 passed; artifacts under crates/ploke-tui/target/test-output/openrouter_matrix
  - Observations (sample from latest metrics):
    - qwen/qwen-2.5-7b-instruct → 404 (no pinned provider slug); marked not validated.
    - deepseek/deepseek-chat-v3.1 (provider_slug=chutes) → 200 but no tool_calls observed in first leg; marked not validated.

Notes
- The matrix helper selects a tools-capable provider slug when available via `fetch_model_endpoints` to reduce 404s.
- The matrix test accepts env controls:
  - PLOKE_LIVE_MODELS (comma-separated model ids)
  - PLOKE_LIVE_MAX_COMBOS (cap combinations)
- Next iteration can add env-driven prompts and extend provider slug pinning.

Next Steps
- Add offline shape assertions for `CompReq` and endpoints response.
- Add an offline `RequestSession` two-leg simulation.
- Iterate matrix prompts and collect token usage when present; add summarizer to aggregate metrics by model/prompt/tool_choice.

