# Date
2026-03-29

# Task Title
Partial Writeup for Live Corpus Run `run-1774827312664`

# Status
Interrupted partial run. This writeup is based on observed persisted artifacts during the session plus the terminal transcript. The original on-disk run directory was no longer present by the time this writeup was drafted, so this is not reconstructed from a final `summary.json`.

## Run Identity

- Run id: `run-1774827312664`
- Intended command: `cargo xtask parse debug corpus --limit 100 --workspace-mode probe`
- Timeout posture in effect: `resolve` and `merge` defaulted to `5` minutes
- End state: operator-interrupted after the corpus command appeared stalled during `block/goose` checkout

## Completion State

- The run did not complete cleanly.
- No top-level `summary.json` was produced.
- The run progressed through `biomejs/biome` and `bitflags/bitflags`.
- The next visible target was `block/goose`, which logged `checkout clone start` and then stopped producing new artifacts or new parse-stage output.
- A later `Ctrl-C` against the background PTY produced `^C`, confirming the foreground corpus command was still attached at that point.

## Failure Snapshot

At the last confirmed triage snapshot, the run had:

- `17` failure occurrences
- `7` distinct report paths / active clusters

Confirmed clusters and outcomes:

1. `resolve|error|Partial parsing success: 28 succeeded, 3 failed`
- Example: `FuelLabs/sway::forc-migrate`
- Classified as existing known limitation `KL-002` (`proc-macro` pre-expansion syntax)

2. `merge|timeout|merge stage exceeded timeout after 300084ms`
- Example: `RustPython/RustPython::vm`
- Treated as novel merge-stage performance timeout

3. `merge|error|Internal state error: Failed to build module tree: Feature not implemented: Duplicate definition path 'crate::untildify:...`
- Example: `TabbyML/tabby::aim-downloader`
- Classified by sub-agent as existing `P3-00-cfg-duplication` module-tree duplicate-path limitation

4. `merge|error|Internal state error: Failed to build module tree: Feature not implemented: Duplicate definition path 'crate::scheduler:...`
- Example: `ankitects/anki::rslib`
- Similar duplicate-path module-tree issue
- Closest known limitation was `P3-00-cfg-duplication`, but not treated as an exact documented match

5. `merge|timeout|merge stage exceeded timeout after 300075ms`
- Example: `astral-sh/ruff::ruff_linter`
- Treated as novel merge-stage performance timeout

6. `discovery|error|Failed to parse manifest`
- Cluster expanded across multiple `bevyengine/bevy` members
- Examples included `benches`, `compile_fail`, `errors`, `build-easefunction-graphs`, `build-templated-pages`, `build-wasm-example`, `ci`, `compile_fail_utils`, `example-showcase`
- Diagnosed as a shared manifest/discovery issue
- Sub-agent noted missing `[package].version` in affected manifests and only a partial resemblance to `requires_bin.md`

7. `merge|timeout|merge stage exceeded timeout after 300083ms`
- Example: `biomejs/biome::biome_js_analyze`
- Treated as novel merge-stage performance timeout

## Operational Issues

The live run surfaced two important operational problems:

1. `corpus-triage --watch` was not robust to partially written workspace summaries.
- It crashed while reading:
- `.../biomejs__biome/workspace_probe/workspace_summary.json`
- Error observed:
- `EOF while parsing a value at line 17 column 24`
- Monitoring continued afterward via direct polling of persisted files rather than the watcher process.

2. Checkout appears to be an unguarded hang point.
- The run later stopped advancing after:
- `[40/100] block/goose checkout clone start`
- No discovery / resolve / merge artifacts were written for `block/goose`
- No clone directory for that repo was present under `tests/fixture_github_clones/corpus`
- This strongly suggests a stall inside checkout itself, likely `git clone` or related fetch work, before stage-level timeout guardrails could apply.

## Notes for Follow-Up

- Stage timeouts for `resolve` and `merge` were functioning as intended during the run.
- Long merges below `300s` still completed normally.
- The next guardrail gap is checkout / clone timeout coverage.
- The triage watcher should be hardened against partially written `workspace_summary.json` files.
- Because the original run directory was later unavailable, any deeper postmortem that needs raw artifacts will require a fresh repro run.
