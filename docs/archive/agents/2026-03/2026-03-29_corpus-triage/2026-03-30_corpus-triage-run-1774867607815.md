# date: 2026-03-30
# task title: corpus triage run-1774867607815 follow-up repros
# task description: add minimal repro coverage for the remaining feasible corpus failures and record the non-repro blockers
# related planning files: docs/active/agents/2026-03-29_corpus-triage/2026-03-30_corpus-live-triage-runbook-v2.md, docs/active/agents/2026-03-29_corpus-triage/2026-03-30_run-1774867607815-post-run-writeup.md

| failure | status | artifact/fixture | notes |
| --- | --- | --- | --- |
| failure-0019 | repro added; validated | `member_cfg_duplicate_mods_repro`; `module_tree_duplicates` | `bytecodealliance/rustix` merge duplicate-path case |
| failure-0038 | repro added; validated | `member_protos_default_index_repro`; `module_tree_duplicates` | `jj-vcs/jj` merge duplicate-path case |
| failure-0043 | repro added; validated | `partial_parsing` | `linera-io/linera-protocol::plugins` partial-parse case |
| failure-0006 | repro added; validated | `manifest_errors` | `bevyengine/bevy::benches` missing `package.version` |
| failure-0042 | repro added; validated | `manifest_errors` | `linera-io/linera-protocol::linera-bridge` missing `[[bin]].path` |
| failure-0020 | not added | existing discovery coverage already exists | `cjpais/Handy` missing `Cargo.toml` is already covered by an existing structured discovery test |
| failure-0018 | blocked | none | checkout timeout; not a stable minimal repro target |
| failure-0035 | blocked | none | checkout timeout; not a stable minimal repro target |
| failure-0045 | blocked | none | merge timeout; not a stable minimal repro target |
| failure-0046 | blocked | none | merge timeout; not a stable minimal repro target |

## Introspection

- Context: the run left only deterministic parser/discovery cases and timeout-only operational failures.
- What worked: keeping the main thread focused on targeted `xtask` exports and repo-name searches was enough to avoid duplicating existing repros.
- Validation: `cargo test -p syn_parser module_tree_duplicates -- --nocapture`, `cargo test -p syn_parser manifest_errors -- --nocapture`, and `cargo test -p syn_parser partial_parsing -- --nocapture` all passed.
- Current recommendation: fold the new repros into the existing grouped files if the team wants fewer test files, but keep the temp-crate partial-parse case separate until the desired fixture shape is agreed.

## Commands

- `cargo xtask --format json parse debug corpus-repro run-1774867607815 --failure failure-0019`
- `cargo xtask --format json parse debug corpus-repro run-1774867607815 --failure failure-0038`
- `cargo xtask --format json parse debug corpus-repro run-1774867607815 --failure failure-0043`
- `cargo xtask --format json parse debug corpus-repro run-1774867607815 --failure failure-0006`
- `cargo xtask --format json parse debug corpus-repro run-1774867607815 --failure failure-0042`
- `cargo xtask parse debug corpus-show run-1774867607815 --target bytecodealliance/rustix --backtrace`
- `cargo xtask parse debug corpus-show run-1774867607815 --target jj-vcs/jj --member lib --backtrace`
- `cargo xtask parse debug corpus-show run-1774867607815 --target linera-io/linera-protocol --member plugins --backtrace`
- `cargo test -p syn_parser module_tree_duplicates -- --nocapture`
- `cargo test -p syn_parser manifest_errors -- --nocapture`
- `cargo test -p syn_parser partial_parsing -- --nocapture`
