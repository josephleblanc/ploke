# Date
2026-03-30

# Task Title
Run 1774867607815 Live Corpus Session

# Task Description
Track the live `parse debug corpus` sweep for 100 repos, record run identity and operational state, and leave a compact handoff for ongoing triage and later repro work.

# Related Planning Files
- [2026-03-30_corpus-live-triage-runbook-v2.md](2026-03-30_corpus-live-triage-runbook-v2.md)
- [2026-03-28_corpus-triage-workflow.md](2026-03-28_corpus-triage-workflow.md)
- [2026-03-30_corpus-repro-report-template.json](2026-03-30_corpus-repro-report-template.json)

## Session Start

- Exact corpus command: `cargo xtask parse debug corpus --limit 100 --workspace-mode probe --artifact-dir xtask/debug_corpus_runs`
- Run id: `run-1774867607815`
- Run directory: `xtask/debug_corpus_runs/run-1774867607815`
- Artifact root: `xtask/debug_corpus_runs` (explicit, same as default)
- Checkout root: `tests/fixture_github_clones/corpus` (default)
- Workspace mode: `probe`
- Limit: `100`
- Timeout settings: defaults in effect unless later noted (`checkout=5m`, `resolve=5m`, `merge=5m`)
- Corpus session id: `85887`
- Triage watcher command: `cargo xtask parse debug corpus-triage run-1774867607815 --watch --interval-secs 15`
- Triage watcher session id: `15111`
- Initial triage snapshot: partial, `0` failures, `0` clusters
- Status: complete

## Notes

- Use persisted artifacts under `xtask/debug_corpus_runs/run-1774867607815/` as the source of truth.
- Keep one writer per pending report path under `xtask/debug_corpus_runs/run-1774867607815/triage/reports/pending/`.
- When failures appear, prefer `index.json` as the live feed and `clusters.json` as dedupe support.
- Operational note: in this run, `triage/clusters.json` is written as a bare JSON array, while `triage/index.json` contains the wrapped `clusters` field. Direct `jq` queries against `triage/clusters.json` should use `.[]` rather than `.clusters[]`.
- First investigated failure: `failure-0001` for `FuelLabs/sway`, member `forc-migrate`, stage `resolve`, cluster `resolve_error_Partial_parsing_success__28_succeeded__3_failed`.
- Current assessment for `failure-0001`: likely raw pre-expansion proc-macro placeholder syntax in `forc-migrate` causing `syn::parse_file` failures (`expected ','`), matching known limitation `KL-002-proc-macro-pre-expansion-syntax.md`.
- Repro selector recorded by sub-agent: `cargo xtask --format json parse debug corpus-repro run-1774867607815 --failure failure-0001`
- Investigated `failure-0002`: `RustPython/RustPython::vm`, stage `merge`, timeout after `300072ms`. Current assessment is merge-stage performance timeout rather than parser invariant failure; no direct known-limitation match recorded.
- Investigated `failure-0003`: `TabbyML/tabby::aim-downloader`, stage `merge`, duplicate definition path rooted at `crate::untildify`. Current assessment matches known limitation `P3-00-cfg-duplication.md`.
- Investigated `failure-0004`: `ankitects/anki::rslib`, stage `merge`, duplicate definition path rooted at `crate::scheduler`. Current assessment is duplicate module-path merge failure with no confirmed direct known-limitation match.
- New live cluster observed: `failure-0005` for `astral-sh/ruff::ruff_linter`, stage `merge`, timeout after `300080ms`; pending report dispatched for investigation.
- Investigated `failure-0005`: `astral-sh/ruff::ruff_linter`, stage `merge`, timeout after `300080ms`. Current assessment is merge-stage performance timeout with no direct known-limitation match.
- New live cluster observed in `bevyengine/bevy`: `failure-0006` (`benches`) and `failure-0007` (`compile_fail`) both at stage `discovery` with error signature `Failed to parse manifest`. Dispatch is cluster-oriented because both failures map to the same pending report path.
- The `bevyengine/bevy` manifest-parse cluster later grew through `failure-0016` without creating a new cluster slug; these remain deduped under `discovery_error_Failed_to_parse_manifest`.
- Investigated `failure-0017`: `biomejs/biome::biome_js_analyze`, stage `merge`, timeout after `300090ms`. Current assessment is a merge-stage performance timeout with no narrower reproduced signature and no direct known-limitation match.
- Investigated `failure-0018`: `block/goose`, stage `checkout`, timeout after `300006ms`. Current assessment is an operational clone timeout rather than a parser or merge invariant failure; no direct known-limitation match recorded.
- New live cluster observed: `failure-0019` for `bytecodealliance/rustix`, stage `merge`, duplicate definition path rooted under `crate::backend::l...`; pending report dispatched for investigation.
- Investigated `failure-0019`: `bytecodealliance/rustix`, stage `merge`, duplicate definition path `crate::backend::libc::c::readwrite_pv64v2`. Current assessment is a cfg-duplication style module-tree collision matching known limitation `P3-00-cfg-duplication.md`.
- New live cluster observed: `failure-0020` for `cjpais/Handy`, stage `discovery`, error signature `Failed to read manifest`; pending report dispatched for investigation.
- Investigated `failure-0020`: `cjpais/Handy`, stage `discovery`, `Failed to read manifest`. Current assessment is a discovery-stage `ENOENT` because the repo root is missing `Cargo.toml`; no direct known-limitation match recorded.
- `failure-0021` (`clockworklabs/SpacetimeDB::bench`, stage `discovery`) extends the existing `discovery_error_Failed_to_parse_manifest` cluster rather than creating a new cluster slug, so no new report dispatch was needed.
- `failure-0022` (`cube-js/cube`, stage `discovery`) extended the existing `discovery_error_Failed_to_read_manifest` cluster; no new report dispatch was needed.
- `failure-0023` (`emilk/egui::external_eventloop_async`, stage `discovery`) extended the existing `discovery_error_Failed_to_parse_manifest` cluster; no new report dispatch was needed.
- `failure-0024` (`eza-community/eza`, stage `discovery`) extended the existing `discovery_error_Failed_to_parse_manifest` cluster; no new report dispatch was needed.
- `failure-0025` (`facebook/flow`, stage `discovery`) extended the existing `discovery_error_Failed_to_read_manifest` cluster; no new report dispatch was needed.
- `failure-0026` (`facebook/relay`, stage `discovery`) and `failure-0027` (`farion1231/cc-switch`, stage `discovery`) also extended the existing `discovery_error_Failed_to_read_manifest` cluster.
- `failure-0028` through `failure-0033` in `firecracker-microvm/firecracker` extended the existing `discovery_error_Failed_to_parse_manifest` cluster for workspace members with manifest-parse failures.
- New live cluster observed: `failure-0034` for `firecracker-microvm/firecracker::vmm`, stage `merge`, timeout after `300058ms`; pending report dispatched for investigation.
- New live cluster observed: `failure-0035` for `google/zerocopy`, stage `checkout`, timeout after `300041ms`; pending report dispatched for investigation.
- New live cluster observed: `failure-0036` for `huggingface/candle::candle-core`, stage `merge`, duplicate definition path rooted at `crate::quantized`; pending report dispatched for investigation.
- New live cluster observed: `failure-0037` for `iced-rs/iced::wgpu`, stage `merge`, duplicate definition path rooted at `crate::image`; pending report dispatched for investigation.
- Investigated `failure-0034`: `firecracker-microvm/firecracker::vmm`, stage `merge`, timeout after `300058ms`. Current assessment is a merge-stage watchdog timeout with no narrower persisted diagnostic and no direct known-limitation match.
- Investigated `failure-0035`: `google/zerocopy`, stage `checkout`, timeout after `300041ms`. Current assessment is an operational clone timeout; no direct known-limitation match recorded.
- Investigated `failure-0036`: `huggingface/candle::candle-core`, stage `merge`, duplicate path `crate::quantized::metal`. Current assessment is a cfg-duplication style module-tree collision matching known limitation `P3-00-cfg-duplication.md`.
- Investigated `failure-0037`: `iced-rs/iced::wgpu`, stage `merge`, duplicate path `crate::image`. Current assessment is a cfg-duplication style module-tree collision matching known limitation `P3-00-cfg-duplication.md`.
- New live cluster observed: `failure-0038` for `jj-vcs/jj::lib`, stage `merge`, duplicate definition path rooted under `crate::protos::de...`; pending report dispatched for investigation.
- Investigated `failure-0038`: `jj-vcs/jj::lib`, stage `merge`, duplicate path `crate::protos::default_index`. Current assessment is a generated-source duplicate-path collision matching known limitation `P3-00-cfg-duplication.md`.
- New live cluster observed: `failure-0039` for `juspay/hyperswitch`, stage `checkout`, timeout after `300098ms`; pending report dispatched for investigation.
- Investigated `failure-0039`: `juspay/hyperswitch`, stage `checkout`, timeout after `300098ms`. Current assessment is an operational clone timeout with no direct known-limitation match.
- New live cluster observed: `failure-0040` for `lbjlaq/Antigravity-Manager`, stage `discovery`, error signature `Failed to read manifest`; this extended an existing discovery cluster and did not need a new dispatch.
- New live cluster observed: `failure-0041` for `leptos-rs/leptos::leptos`, stage `merge`, duplicate definition path `crate::logging`; pending report dispatched for investigation.
- Investigated `failure-0039`: `juspay/hyperswitch`, stage `checkout`, timeout after `300098ms`. Current assessment is an operational clone timeout; no direct known-limitation match recorded.
- `failure-0040` (`lbjlaq/Antigravity-Manager`, stage `discovery`) extended the existing `discovery_error_Failed_to_read_manifest` cluster and did not require a new report dispatch.
- New live cluster observed: `failure-0041` for `leptos-rs/leptos::leptos`, stage `merge`, duplicate definition path rooted under `crate::logging...`; pending report dispatched for investigation.
- Investigated `failure-0041`: `leptos-rs/leptos::leptos`, stage `merge`, duplicate path `crate::logging`. Current assessment is a structural duplicate-module collision between inline `pub mod logging { ... }` and `logging.rs`; no direct known-limitation match recorded.
- `failure-0042` (`linera-io/linera-protocol::linera-bridge`, stage `discovery`) extended the existing `discovery_error_Failed_to_parse_manifest` cluster and did not require a new report dispatch.
- New live cluster observed: `failure-0043` for `linera-io/linera-protocol::plugins`, stage `resolve`, `Partial parsing success: 2 succeeded, 1 failed`; pending report dispatched for investigation.
- New live cluster observed: `failure-0044` for `linera-io/linera-protocol::linera-service`, stage `merge`, duplicate definition path rooted under `crate::cli...`; pending report dispatched for investigation.
- Investigated `failure-0043`: `linera-io/linera-protocol::plugins`, stage `resolve`, `Partial parsing success: 2 succeeded, 1 failed`. Current assessment is a pre-expansion placeholder-syntax parse failure matching known limitation `KL-002-proc-macro-pre-expansion-syntax.md`.
- Investigated `failure-0044`: `linera-io/linera-protocol::linera-service`, stage `merge`, duplicate path `crate::cli`. Current assessment is a structural duplicate-module collision between the library `cli` module and the binary-target `src/cli` tree; no direct known-limitation match recorded.
- New live cluster observed: `failure-0045` for `microsoft/windows-rs::sys`, stage `merge`, timeout after `300093ms`; pending report dispatched for investigation.
- Investigated `failure-0045`: `microsoft/windows-rs::sys`, stage `merge`, timeout after `300093ms`. Current assessment is a merge-stage watchdog timeout with no narrower persisted diagnostic and no direct known-limitation match.
- Final run state: corpus sweep completed for all `100` targets under `run-1774867607815`.
- Final triage snapshot: `47` failures, `21` clusters, `21` pending reports.
