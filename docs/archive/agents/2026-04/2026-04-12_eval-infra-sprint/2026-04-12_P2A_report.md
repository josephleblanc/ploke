# P2A Report - Phase 2 Entry Run Planning

- date: 2026-04-12
- owning_branch: `refactor/tool-calls`
- task: `P2A` - Phase 2 Entry Run Planning
- status: accepted
- related artifacts:
  - [P2A packet](./2026-04-12_P2A_phase-2-entry-run-planning.md)
  - [Current focus](../../CURRENT_FOCUS.md)
  - [Eval infra sprint control plane](./2026-04-12_eval-infra-sprint-control-plane.md)
  - [Target capability registry](../../workflow/target-capability-registry.md)
  - [Hypothesis registry](../../workflow/hypothesis-registry.md)
  - [Priority queue](../../workflow/priority-queue.md)
  - [Evidence ledger](../../workflow/evidence-ledger.md)
  - [Phased execution plan](../../plans/evals/phased-exec-plan.md)

## Current State

| task_id | status | owner | layer/workstream | packet_link | latest_report_link | next_action |
| --- | --- | --- | --- | --- | --- | --- |
| `P2A` | `accepted` | orchestrator | `A2` / `A3` / `H0` | [P2A](./2026-04-12_P2A_phase-2-entry-run-planning.md) | [P2A report](./2026-04-12_P2A_report.md) | open `P2B` as the next bounded packet |
| `P2B` | `ready` | orchestrator | `A2` | [P2B](./2026-04-12_P2B_ripgrep-a2-validation.md) | none yet | validate ripgrep mixed-edition parse readiness before any formal baseline/control batch |

## Acceptance Criteria

1. The packet produces a bounded recommendation for the first Phase 2 run-planning slice, with explicit target or subset candidates and linked run-policy annotations from the target capability registry.
2. The packet identifies the concrete blockers, if any, that still prevent a fair formal baseline/control batch from starting, with specific reference to the current `A2`, `A3`, and manifest/validity workflow state.
3. The packet leaves behind one clear next packet recommendation.

## What Changed

- Chose a bounded Phase 2 entry recommendation instead of jumping directly to live baseline runs.
- Identified the first candidate family as ripgrep-only, but kept it in a pre-baseline gating posture until `A2` is revalidated.
- Seeded `P2B` as the next packet: validate dual-`syn` readiness on ripgrep and then update target/hypothesis status from evidence.
- Updated the priority queue so the next blocker order is explicit: `A2` validation, then validity-guard policy, then manifest convergence.

## Claims

- [1] The first bounded Phase 2 slice should be a ripgrep-focused pre-baseline gating pass, not a formal shell-only vs. structured baseline batch yet.
- [2] The best-evidenced candidate set today is narrow:
  `BurntSushi__ripgrep-1294` as the `A2` mixed-edition parser sentinel,
  `BurntSushi__ripgrep-2209` as the known-good replay/introspection reference artifact,
  and broader ripgrep instances only after the `A2` sentinel is revalidated.
- [3] Three blockers still prevent a fair formal baseline/control batch:
  `A2` readiness is still explicitly at risk,
  `A3` validity-guard threshold policy is still undecided,
  and immutable-manifest provenance is still split across multiple artifacts.
- [4] The next packet should be `P2B`, a narrow `A2` validation pass over ripgrep mixed-edition readiness, because target fairness precedes baseline scheduling.

## Evidence

- file/artifact: [phased-exec-plan.md](../../plans/evals/phased-exec-plan.md)
  Phase 2 requires baselines and controls only after lower-layer validity is acceptable.
- file/artifact: [hypothesis-registry.md](../../workflow/hypothesis-registry.md)
  `A2` is still "at risk - solution implemented, validation pending"; `A4`/`A5` are validated.
- file/artifact: [priority-queue.md](../../workflow/priority-queue.md)
  Current queued blockers already include validity-guard policy and manifest convergence, which remain relevant after `P2A`.
- file/artifact: [evidence-ledger.md](../../workflow/evidence-ledger.md)
  Run provenance is still split across multiple artifacts rather than a converged immutable manifest.
- file/artifact: [target-capability-registry.md](../../workflow/target-capability-registry.md)
  ripgrep is the only concrete resolved-blocker example; macro-heavy and very-large targets remain non-default categories.
- file/artifact: [bug: syn 2 fails on Rust 2015 bare trait objects](../../bugs/2026-04-10-syn-2-fails-on-rust-2015-bare-trait-objects.md)
  documents `BurntSushi__ripgrep-1294` as the mixed-edition parser sentinel and the reason `A2` remains unvalidated.
- file/artifact: [recent-activity.md](../../workflow/handoffs/recent-activity.md)
  confirms accepted `P0A`-`P0F`, accepted sidecars, and the newly integrated target capability registry.
- file/artifact: `crates/ploke-eval/tests/introspection_integration.rs`
  points to `BurntSushi__ripgrep-2209` as the durable reference artifact used by introspection/replay tests.

## unsupported_claims

- No claim is made that any formal Phase 2 shell-only/control baseline batch is ready to launch immediately.
- No claim is made that ripgrep-wide target readiness is fully restored beyond the need for the documented `A2` sentinel revalidation.

## not_checked

- No live benchmark runs were launched.
- No parser validation tests or eval commands were executed in this packet.
- No new target family beyond ripgrep was elevated to baseline candidacy because the current workflow record did not provide equally strong readiness evidence.

## Risks

- The candidate set is intentionally conservative; there may be additional viable targets not yet elevated into the workflow record.
- `P2B` may validate the ripgrep sentinel but still leave broader target-subset selection ambiguous if other ripgrep instances show different operational issues.
- After `P2B`, the programme may still need an `A3` validity-guard packet before any formal baseline/control batch is fair to interpret.

## Open Questions

- After `P2B`, should the next packet be validity-guard policy adoption or run-manifest convergence if both remain partially blocking?
- Should ripgrep task-level entries be added to the target capability registry once `P2B` clarifies which instances are baseline candidates versus sentinel-only probes?

## Resume Here

1. Open [P2B](./2026-04-12_P2B_ripgrep-a2-validation.md).
2. Validate the ripgrep mixed-edition parser sentinel and update the hypothesis/registry status from evidence.
3. If `P2B` is accepted, open the next narrow blocker packet for validity-guard policy or manifest convergence before baseline execution planning.
