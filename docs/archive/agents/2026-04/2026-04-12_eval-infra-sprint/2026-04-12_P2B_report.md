# P2B Report - Ripgrep A2 Validation

- date: 2026-04-12
- owning_branch: `refactor/tool-calls`
- task: `P2B` - Ripgrep A2 Validation
- status: accepted
- related artifacts:
  - [P2B packet](./2026-04-12_P2B_ripgrep-a2-validation.md)
  - [P2A report](./2026-04-12_P2A_report.md)
  - [Current focus](../../CURRENT_FOCUS.md)
  - [Eval infra sprint control plane](./2026-04-12_eval-infra-sprint-control-plane.md)
  - [Hypothesis registry](../../workflow/hypothesis-registry.md)
  - [Target capability registry](../../workflow/target-capability-registry.md)
  - [Priority queue](../../workflow/priority-queue.md)
  - [bug: syn 2 fails on Rust 2015 bare trait objects](../../bugs/2026-04-10-syn-2-fails-on-rust-2015-bare-trait-objects.md)

## Current State

| task_id | status | owner | layer/workstream | packet_link | latest_report_link | next_action |
| --- | --- | --- | --- | --- | --- | --- |
| `P2B` | `accepted` | orchestrator | `A2` | [P2B](./2026-04-12_P2B_ripgrep-a2-validation.md) | [P2B report](./2026-04-12_P2B_report.md) | open `P2C` for validity-guard policy before formal baseline/control scheduling |
| `P2C` | `ready` | orchestrator | `A3` / `H0` | [P2C](./2026-04-12_P2C_validity-guard-policy.md) | none yet | decide and document the current formal-run validity-guard threshold policy |

## Acceptance Criteria

1. The packet produces concrete evidence for whether the documented ripgrep mixed-edition parser blocker is actually resolved in the current code path.
2. The packet states whether ripgrep should remain a sentinel-only target, become a baseline candidate, or move to an intermediate registry state such as `resolved_pending_reentry`.
3. The packet updates or recommends updates to the `A2` hypothesis and target capability registry without overstating what was checked.

## What Changed

- Ran the two targeted edition-2015 parser repros that encode the previously documented failure shapes.
- Ran a fresh temp-root `BurntSushi__ripgrep-1294` validation through the current `ploke-eval` CLI path instead of relying on the ambiguous older run directory.
- Confirmed that indexing now completes cleanly for the ripgrep sentinel without emitting a new parse-failure artifact for `globset` or the prior mixed-edition failure path.
- Promoted the next blocker from `A2` re-entry validation to `A3` validity-guard policy definition by seeding `P2C`.

## Claims

- [1] The previously documented ripgrep mixed-edition parser blocker is not supported by the current code path anymore.
- [2] Ripgrep should now be treated as a Phase 2 baseline candidate rather than as a sentinel-only blocked target, while broader formal scheduling remains separately blocked by validity-guard and manifest-convergence work.
- [3] This packet supports narrowing the ripgrep-specific `A2` blocker from "validation pending" to "revalidated sentinel," but it does not by itself validate all of `A2` across target families or recall/coverage questions beyond this sentinel.

## Evidence

- Targeted parser repro command:
  - `cargo test -p syn_parser repro_try_run_phases_and_resolve_parses_2015 -- --nocapture`
  - result: `2 passed; 0 failed`
  - interpretation: the two edition-2015 repros for bare trait objects and `async` identifiers now pass on the current parser path
- Fresh ripgrep sentinel validation:
  - temp eval home: `/tmp/ploke-eval-p2b-cli.003I7J`
  - prepare command:
    `PLOKE_EVAL_HOME=/tmp/ploke-eval-p2b-cli.003I7J cargo run -p ploke-eval -- prepare-msb-single --dataset-key ripgrep --instance BurntSushi__ripgrep-1294 --repo-cache /home/brasides/.ploke-eval/repos`
  - run command:
    `PLOKE_EVAL_HOME=/tmp/ploke-eval-p2b-cli.003I7J cargo run -p ploke-eval -- run-msb-single --instance BurntSushi__ripgrep-1294`
  - fresh run root:
    `/tmp/ploke-eval-p2b-cli.003I7J/runs/BurntSushi__ripgrep-1294`
- Fresh run artifacts inspected:
  - [indexing-status.json](/tmp/ploke-eval-p2b-cli.003I7J/runs/BurntSushi__ripgrep-1294/indexing-status.json)
    reports `completed` with detail `Indexing completed through the full app command path.`
  - [snapshot-status.json](/tmp/ploke-eval-p2b-cli.003I7J/runs/BurntSushi__ripgrep-1294/snapshot-status.json)
    reports `completed` and points at the fresh `final-snapshot.db`
  - [repo-state.json](/tmp/ploke-eval-p2b-cli.003I7J/runs/BurntSushi__ripgrep-1294/repo-state.json)
    shows a clean checkout at `392682d35296bda5c0d0cccf43bae55be3d084df`
  - [execution-log.json](/tmp/ploke-eval-p2b-cli.003I7J/runs/BurntSushi__ripgrep-1294/execution-log.json)
    exists for the fresh run and was inspected alongside the emitted artifact set
  - negative artifact evidence:
    no `parse-failure.json` was emitted in the fresh run directory
- Existing ambiguous artifact note:
  - the older `~/.ploke-eval/runs/BurntSushi__ripgrep-1294` directory still mixes a stale `parse-failure.json` with a later `indexing-status.json` that says `completed`, so this packet deliberately used a fresh temp-root run instead of inferring from that mixed state

## unsupported_claims

- I did not prove ripgrep-wide parse coverage or query recall beyond the documented mixed-edition sentinel path.
- I did not prove the exact "all 9 ripgrep crates indexed" condition from a fresh `1294` `SetupPhase` artifact because `run-msb-single` did not emit a `record.json.gz` / `SetupPhase` for this bounded validation pass.
- I did not prove that every other mixed-edition Rust target is now fair by default; this packet is ripgrep-sentinel scoped.

## not_checked

- No formal shell-only vs. structured baseline batch was run.
- No provider-policy or validity-guard threshold packet was executed here.
- I did not query the fresh `final-snapshot.db` directly for a crate-count roll-up.

## Risks

- The fresh sentinel run is strong evidence that the old `globset` parser blocker is gone, but it is still one sentinel run; broader `A2` health needs later coverage/recall work rather than assumption by analogy.
- The temp-home prepare path was not clean-room robust by default; the worker had to seed dataset cache state under `/tmp` to make the command succeed. That is relevant to tooling ergonomics, but it does not undermine the ripgrep parser result itself.
- Because the bounded run stopped at the `run-msb-single` artifact set and did not emit a `record.json.gz`, the strongest direct 9-crate check still comes from the existing `BurntSushi__ripgrep-2209` introspection-backed artifact family rather than this fresh `1294` directory.

## Open Questions

- Should the next packet adopt explicit numeric validity guards directly in workflow docs, or should it require an EDR / experiment-config update before those thresholds become operationally binding?
- After `P2C`, should the following packet target manifest convergence first or a very small ripgrep baseline/control execution plan first?

## Resume Here

1. Open [P2C](./2026-04-12_P2C_validity-guard-policy.md).
2. Decide and document the current formal-run validity-guard threshold policy.
3. Keep ripgrep in the Phase 2 candidate set while leaving broader formal baseline/control scheduling blocked on `P2C` and manifest convergence.
