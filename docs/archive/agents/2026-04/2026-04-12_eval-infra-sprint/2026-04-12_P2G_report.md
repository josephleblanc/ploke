# P2G Report

- Date: 2026-04-12
- Packet: [P2G - Runner Arm Surface](./2026-04-12_P2G_runner-arm-surface.md)
- Status recommendation: accepted
- Layer/workstream: `A3` / `A4` / `H0`

## Claims

- `P2G` met its primary acceptance boundary inside `crates/ploke-eval/` only.
- `ploke-eval` now persists explicit arm provenance for shell-only control vs structured-current-policy treatment, so formal packet execution no longer relies on implicit command semantics alone.
- `ploke-eval` now persists selected endpoint provenance, including `quantization` when OpenRouter exposes it, in both `execution-log.json` and `record.json.gz`.
- The first formal ripgrep packet is now executable at the runner-surface level; subsequent blockers observed in live runs were provider/runtime/budget/model-behavior issues, not arm-surface ambiguity.
- The active next question is a bounded CLI-first diagnostic packet over completed `x-ai/grok-4-fast` / `xai` treatment retries, not more runner-surface work.

## Evidence

- Runner-surface implementation and verification stayed inside `crates/ploke-eval/**`:
  - [runner.rs](/home/brasides/code/ploke/crates/ploke-eval/src/runner.rs)
  - [record.rs](/home/brasides/code/ploke/crates/ploke-eval/src/record.rs)
  - [cli.rs](/home/brasides/code/ploke/crates/ploke-eval/src/cli.rs)
  - [setup_phase_integration.rs](/home/brasides/code/ploke/crates/ploke-eval/tests/setup_phase_integration.rs)
- Targeted verification found the runner surface explicit enough to distinguish control vs treatment honestly and did not identify a smaller prerequisite inside the cited scope.
- Real execution evidence:
  - control run completed under `moonshotai/kimi-k2.5` / `baseten` and wrote the expected artifacts
  - treatment runs exercised real chat traffic under `moonshotai/kimi-k2.5` / `baseten`, `moonshotai/kimi-k2.5` / `modelrun`, and `x-ai/grok-4-fast` / `xai`
  - provider-side `429`, budget-side `402`, and transient `502` failures were observed as live execution blockers after the arm surface was already working
- Endpoint provenance evidence:
  - `modelrun` treatment artifacts captured `selected_endpoint.quantization = "fp4"`
  - `x-ai/grok-4-fast` / `xai` treatment artifacts captured endpoint provenance with `quantization = "unknown"`
- CLI-first introspection on completed `grok-4-fast` / `xai` retries showed the authoritative discrepancy is behavioral:
  - one completed retry reached `non_semantic_patch` and applied a patch
  - another completed retry never entered a patch tool path
  - the discrepancy is best classified as `MODEL_STRATEGY` with secondary `TOOL_SEMANTICS`, not artifact-accounting inconsistency

## Unsupported Claims

- This report does not claim benchmark success on `BurntSushi__ripgrep-1294`; the packet only addresses runner-surface readiness and its immediate follow-on evidence.
- This report does not claim endpoint `quantization` will always be populated; the runner now preserves it when the provider metadata exposes it.

## Not Checked

- No new packetized follow-up doc has been authored yet for the CLI-first diagnostic step on completed `grok-4-fast` / `xai` retries.
- No downstream workflow consumer audit was performed for the new endpoint-provenance fields beyond targeted runner-local verification.

## Risks

- Completed treatment retries still show significant behavioral variance under fixed controls, so the next interpretation packet must separate outcome from validity/health metrics and avoid over-reading a small sample.
- Some providers expose endpoint quantization while others report `unknown`, so cross-provider treatment comparisons still require explicit provenance review.

## Next Action

- Accept `P2G` as complete.
- Open a bounded CLI-first diagnostic packet on the completed `BurntSushi__ripgrep-1294` `x-ai/grok-4-fast` / `xai` treatment retries using `inspect tool-calls`, `inspect turn --show messages`, and `conversations` as the authority layer.
