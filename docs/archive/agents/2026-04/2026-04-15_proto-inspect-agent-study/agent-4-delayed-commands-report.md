I expect the two `ploke-eval inspect proto` commands to show the same core shape from two different distances: the all-runs view should make coverage imbalance and issue concentration obvious, while the single-run view should expose whether one run is internally coherent or mostly degraded by anchor mismatch and partial segment usability. The most useful thing to learn is not the exact counts by themselves, but whether the output cleanly separates “fully trustworthy,” “partially usable,” and “structurally degraded” runs without forcing me to read raw artifacts.

The likely ambiguity is that the current aggregate may mix signal with presentation detail: the summary table can rank runs, but it may not yet tell me what kind of failure dominates, and the single-run graph may compress segment-review presence, segment-review mismatch, and call-level severity into one visual story. The information that matters most is the breakdown behind the top rows, especially which gaps are missing coverage versus anchor mismatch, whether issues cluster by a few segments or spread across the whole run, and whether the visual encoding makes those distinctions faster to see than the raw numbers.
