//! Test cases for use statement parsing

// Simple imports
use std::collections::HashMap;
use std::io::{Read, Write};

// Aliases
use std::fmt as formatting;
use std::collections::HashMap as Map;

// Nested groups
use std::sync::{atomic::{AtomicBool, Ordering}, Arc};

// Glob imports
use std::prelude::v1::*;

// Relative paths
use super::parent_module;
use crate::sibling_module;

// Complex cases
use std::{
    collections::HashMap,
    io::{self, Read},
    path::{Path, PathBuf},
};
