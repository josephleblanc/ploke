[package]
name = "fixture_conflation"
version = "0.1.0"
edition = "2021"
description = "Test fixture specifically designed to expose TypeId conflation issues, especially with generics (T) and Self types across different scopes and files."

[dependencies]
# No dependencies needed for this fixture
