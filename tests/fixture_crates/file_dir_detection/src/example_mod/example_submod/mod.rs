// Retain existing structure but add an item

// Public files in same directory (will be deleted later)
// pub mod submod_sibling_one;
// pub mod submod_sibling_two;

// Item directly within example_submod
pub fn item_in_example_submod() {}
