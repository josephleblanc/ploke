Review: 8836d215 — add update for model_browser to use new strict types for model fields

Scope
- File: crates/ploke-tui/src/app/view/components/model_browser.rs
- Summary: Migrates UI model browser to use new strong types (`ModelId`, `ModelName`) and adjusts string handling for display.

Build & Lints
- cargo check (-p ploke-tui, features: test_harness,llm_refactor): 27 errors, 10 warnings.
  - Notable unrelated breakages: `ModelId` vs `String` mismatches in `app/{mod.rs,events.rs}`, private `llm2::types::newtypes` re-exports used in `commands/exec.rs`, outdated `LLMParameters` fields, and an event variant rename `Llm`→`Llm2`.
  - Due to these, readiness is not established; this review treats the change as isolated.
- rustfmt (file-only): not formatted per rustfmt; suggested reordering of some `use` statements, whitespace fixes, minor signature wrapping.
  - Command: `rustfmt --edition 2021 --check crates/.../model_browser.rs` → non‑zero exit (diff available locally).
- clippy: skipped (crate fails to compile).

Code Review Notes
- Strong typing: Good use of `ModelId` and `ModelName`. `format!("{} — {}", it.id, name.as_str())` relies on `Display` for `ModelId` (implemented) and explicit `as_str()` for `ModelName`.
  - Consider implementing `Display` and/or `Deref<Target=str>` for `ModelName` to allow direct `{}` formatting and reduce `.as_str()` plumbing.
- Title construction:
  - When `name` is empty or `None`, the fallback `it.id.to_string()` is correct. With `Display` on `ModelName`, this can simplify.
- Pricing fields:
  - `input_cost`/`output_cost` are `Option<f64>`. Given the ongoing strict-typing refactor, consider newtypes for units (e.g., `UsdPerToken`, `UsdPerMillionTokens`) to prevent unit mix-ups and to centralize scaling logic (display vs. storage).
  - Provider breakdown multiplies by 1_000_000 and shows three decimals but lacks unit labeling. Add an explicit unit to avoid ambiguity.
- Doc comment:
  - Incomplete: `/// input cost converted from USD/token to` — please complete this sentence and state the internal unit and the displayed unit.
- Minor perf/alloc:
  - `it.context_length.map(|v| v.to_string()).unwrap_or_else(|| "-".to_string())` allocates; not critical, but you could assemble the line via `Span`s to avoid temporary `String`s if this runs often. Not blocking.

Suggested Patch (preview; not applied)
- Clarify pricing units in UI and finish doc comment.
- Minor formatting changes match rustfmt suggestions.

Diff (conceptual):

--- a/crates/ploke-tui/src/app/view/components/model_browser.rs
+++ b/crates/ploke-tui/src/app/view/components/model_browser.rs
@@ struct ModelBrowserItem
-    /// input cost converted from USD/token to 
+    /// Input cost in USD per token (displayed as USD per 1M tokens).
     pub input_cost: Option<f64>,
@@ provider breakdown rendering
-                            format!(
-                                "      - {}  tools={}  ctx={}  pricing: in={} out={}",
+                            format!(
+                                "      - {}  tools={}  ctx={}  pricing (USD/1M tok): in={} out={}",
                                 p.name.as_ref(),
                                 p.supports_tools,
                                 format_args!("{:.0}", p.context_length),
                                 format_args!("{:.3}", p.input_cost * 1_000_000.0),
-                                format_args!("{:.3}", p.output_cost * 1_000_000.0                                             ),
+                                format_args!("{:.3}", p.output_cost * 1_000_000.0),
                             ),

Follow-ups (optional)
- Add `impl Display for ModelName` or `Deref<Target=str>` on the `newtype_arcstr!` macro to streamline UI formatting.
- Introduce strongly typed pricing units:
  - For example: `#[serde(transparent)] struct UsdPerToken(f64);` plus helpers: `fn per_million(&self) -> f64 { self.0 * 1_000_000.0 }`.
  - Update `Endpoint.pricing` and UI to use these types to encode intent and avoid future scaling mistakes.

Evidence
- Check run: `cargo check -p ploke-tui --features "test_harness llm_refactor"` → 27 errors, 10 warnings (workspace-local, unrelated to this file).
- Formatting check: `rustfmt --edition 2021 --check .../model_browser.rs` returned non‑zero; small reorders/whitespace only.

Readiness
- Not established (crate fails to compile and tests not executed). Once upstream breakages are addressed, I can re-run checks and clippy and record results under `target/test-output/...` as needed.
