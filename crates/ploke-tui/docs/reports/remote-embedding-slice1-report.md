# Remote Embedding — Slice 1 Report (2025-11-17)

## Summary
- Planning + onboarding refresh completed. New `contributor_onboarding.md` plus implementation log entries 026/027 give contributors a deterministic path through the plans/code.
- Multi-embedding fixtures verified via `cargo xtask verify-fixtures --multi-embedding`; evidence stored under `target/test-output/embedding/fixtures/multi_embedding_fixture_verification.json` (telemetry metadata now emitted automatically).
- `slice1-schema.json` artifact is up-to-date with the latest test + fixture runs, but schema-module edits are still required before declaring Slice 1 ready.

## Feature flag state
- Compile-time: `ploke-db`, `ploke-transform`, `ploke-embed`, `ploke-rag`, and `ploke-tui` now expose the `multi_embedding_schema/db/runtime/release/kill_switch` ladder. The legacy `multi_embedding_experiment` flag aliases `multi_embedding_schema` temporarily so downstream tools keep working while they migrate.
- Runtime: kill switch not yet wired because runtime-facing work has not started for Attempt 002.

## Evidence collected
| Artifact | Generated by | Notes |
| --- | --- | --- |
| `target/test-output/embedding/fixtures/multi_embedding_fixture_verification.json` | `cargo xtask verify-fixtures --multi-embedding` (2025-11-17T16:23:04Z) | Confirms 12 metadata relations, 48 vector relations, 183 metadata rows, 732 vector rows in schema-tagged backup (includes feature flag metadata). |
| `target/test-output/embedding/slice1-schema.json` | Combined telemetry run (see "Tests executed") | Summarizes commands, pass counts, and feature flags for Slice 1 evidence; marked `live=false` because no remote providers were called. |

## Tests executed
- `cargo test -p ploke-db multi_embedding --features multi_embedding_schema` ✅ (13 tests)
- `cargo test -p ploke-test-utils --features multi_embedding_schema tests::seeds_multi_embedding_relations_for_fixture_nodes` ✅ (1 integration test asserting metadata/vector seeding)
- `cargo xtask verify-fixtures --multi-embedding` ✅ (metadata/vector relation verification)

## Phase B sub-steps status (per experimental_fixtures_plan.md)
- **B1 – metadata helpers**: ✅ Complete. `ploke-db::multi_embedding` module exposes reusable specs; `ploke-test-utils` helpers seed metadata rows.
- **B2 – vector seeding + tests**: ✅ Complete. Integration test `seeds_multi_embedding_relations_for_fixture_nodes` passes under `multi_embedding_schema`.
- **B3 – fixture regeneration & verify command**: ✅ Complete. `cargo xtask verify-fixtures --multi-embedding` validates multi-embedding backups; fixture hashes recorded.
- **B4 – telemetry + documentation**: ✅ Complete. Telemetry artifacts (`multi_embedding_fixture_verification.json`, `slice1-schema.json`) referenced in this report and implementation log.
- **B5 – test review & strictness audit**: ✅ Complete. Tests in `crates/ploke-db/src/helpers.rs` restored to strict behavior (see `governance/test_changes_review.md`). No outstanding warnings.

## Outstanding work for Slice 1 readiness
1. Align the runtime/ingest schema modules with the new helper traits (e.g., `crates/ingest/ploke-transform/src/schema/` still lacks `embedding_vectors.rs`). **Note:** Per runtime-owned embeddings decision, this is deferred to runtime helpers rather than ingest schemas.
2. Migrate any remaining `multi_embedding_experiment` cfg usage to the new feature names and remove the alias once Slice 1 stabilizes.
3. Annotate `execution_plan.md` and `required-groundwork.md` with links to the onboarding doc + telemetry artifacts when Slice 1 code lands.
