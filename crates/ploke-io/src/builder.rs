/*
// Target module for IoManagerBuilder and fd-limit computation

#[derive(Default, Debug)]
pub struct IoManagerBuilder {
    // semaphore_permits: Option<usize>,
    // fd_limit: Option<usize>,
    // roots: Vec<PathBuf>,
}

impl IoManagerBuilder {
    // pub fn with_semaphore_permits(self, permits: usize) -> Self { ... }
    // pub fn with_fd_limit(self, fd_limit: usize) -> Self { ... }
    // pub fn with_roots<I, P>(self, roots: I) -> Self
    // where I: IntoIterator<Item = P>, P: Into<PathBuf>
    // { ... }
    // pub fn build(self) -> IoManagerHandle { ... }
}

// pub(crate) fn compute_fd_limit_from_inputs(soft_nofile: Option<u64>, env_override: Option<usize>, builder_override: Option<usize>) -> usize { ... }
*/
