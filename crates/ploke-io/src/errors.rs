/*
// Target module for error types and mapping

#[derive(Debug, Error, Clone)]
pub enum RecvError {
    // SendError,
    // RecvError,
}

#[derive(Debug, Error, Clone)]
pub enum IoError {
    // Recv(#[from] RecvError),
    // ContentMismatch { name: String, id: uuid::Uuid, file_tracking_hash: uuid::Uuid, namespace: uuid::Uuid, path: PathBuf },
    // ParseError { path: PathBuf, message: String },
    // OutOfRange { path: PathBuf, start_byte: usize, end_byte: usize, file_len: usize },
    // ShutdownInitiated,
    // FileOperation { operation: &'static str, path: PathBuf, source: Arc<std::io::Error>, kind: std::io::ErrorKind },
    // Utf8 { path: PathBuf, source: std::string::FromUtf8Error },
    // InvalidCharBoundary { path: PathBuf, start_byte: usize, end_byte: usize },
}

// impl From<IoError> for ploke_error::Error { ... }
*/
