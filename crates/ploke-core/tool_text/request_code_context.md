Use request_code_context sparingly and only once for initial broad exploration: retrieve code snippets from the indexed workspace up to a token budget when you have partial clues like likely identifiers, module names, file names, or error/type hints, and no precise location yet. Do not use it for iterative refinement, repeated searches, or further exploration after the first call—doing so risks inefficient thrashing and irrelevant results. After invoking it, evaluate the results immediately: if it yields 0 snippets, irrelevant broad matches, or partial context that doesn't resolve your needs, stop using this tool entirely and pivot to more targeted alternatives to gather additional context. Narrow with exact symbols via code_item_lookup for definitions, use locate_target for precise file/module hunting, switch to inspect_candidate, list_dir, or read_file once an area is suspected, or apply refine_search to improve your clues without repetition. Repeated attempts indicate a flawed strategy; reassess and commit to other tools to avoid search thrash and focus on effective context gathering.