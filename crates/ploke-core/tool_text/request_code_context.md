Use `request_code_context` **exclusively once** at the absolute outset of context gathering, and only when you possess highly tentative, partial clues—such as vague identifiers, module names, file names, or error/type hints—without any precise location or path information available. This tool should fetch broad code snippets from the indexed workspace up to a strict token budget for initial reconnaissance. **Do not use it under any circumstances for verification, expansion, refinement, or follow-up; repeated invocations guarantee inefficient thrashing, redundant outputs, and stalled progress.** After this one-time use, rigorously assess the results: if zero snippets are returned, matches appear irrelevant or noisy, or the context remains inadequate for next steps, **halt all further attempts with this tool immediately** and pivot without hesitation to targeted alternatives. Prioritize `code_item_lookup` for precise symbol definitions and lookups; `locate_target` for file or module pinpointing; `refine_search` to iteratively sharpen your search criteria; and `inspect_candidate`, `list_dir`, or `read_file` to drill into specific candidates once a viable lead emerges. Treat any recurring impulse to reuse `request_code_context` as a critical signal of flawed reasoning—force a strategic reset by focusing solely on these precise, non-thrashing tools to maintain momentum in context acquisition.